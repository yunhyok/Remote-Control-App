# Remote Monitor project entry point

Read [HANDOFF.md](HANDOFF.md) first. It contains the project purpose, current v0.1.51 work, verified field results, constraints, source map, build commands and next task. Then read [README.md](README.md) and [SLAVE-TEST.md](SLAVE-TEST.md). [PROJECT-REVIEW.md](PROJECT-REVIEW.md) is dated history, not a current task list.

No Codex-specific tools or plugins are required to build this repository. Keep proprietary screenshots, Output text, logs, pairing credentials and local model settings out of Git. Current field testing is Slave-only. The user skipped v0.1.50 field testing; do not request it again. v0.1.51 restores fresh body verification before any cursor move/click, adds cooperative input-worker cancellation, refreshes the clipboard baseline immediately before Ctrl+C, and fixes repeated-line comparison and bracket-prefixed transcript loss. It is field-unverified; check HANDOFF for the actual build/test status.

The current body detector can reject selected/highlighted Output text. The short field procedure is one manual copy for learning, one user click to clear selection after learning, then one automatic-copy attempt. Do not call this continuous unattended copying: selection remains after Ctrl+A/C, and selected-state support is a follow-up. An automatic failure is reported without another manual-copy prompt; previous full text remains PREVIOUS_CAPTURE with its original buffer_received_utc, never a fresh success. transcript_format_validated means response-format validation only, not OCR accuracy. Optional model comparison reuses the same OCR PNG. Do not repeat proven Master/mobile transport tests, resize trials, or long waits without a relevant need. Keep the application name and version visible, run the appropriate Windows checks, and update HANDOFF.md when the state changes.

Deliver through the CI workflow_dispatch release_tag job as a new version-matching rc pre-release, with the Slave-only ZIP direct URL and SHA256. Existing tags/releases are immutable; use a new rc number after a partial publication failure, never replace assets under an existing tag.
