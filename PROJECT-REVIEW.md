# Remote Monitor v0.1.33 — 진행 검토와 다음 개발 준비

> **시점별 이력 문서입니다. 최신 기준은 v0.1.50(2026-09-13 main 병합)의 [HANDOFF.md](HANDOFF.md)입니다.** 아래의 “현재”, “미구현”, “커밋/푸시 없음”은 각 절을 작성한 당시의 상태입니다. v0.1.34 이후 후속 절을 포함하며 v0.1.48과 GitHub 인수인계는 HANDOFF에서 확인합니다. `dist/` 검증 기록과 원문 로그는 로컬에만 있었으며 이 저장소에 포함하지 않습니다.

검토일: 2026-09-10. 현재 코드, 배포 검증 기록, 실제 Win7 Master / Win11 Slave 로그, 사용자의 휴대폰 수신 확인을 함께 검토했다.

## 1. 결론

**기본 상태 요청·응답 통신은 검증 완료로 판단한다. 이제 중심 과제는 통신 진단이 아니라 Slave에서 의미 있는 업무 상태를 읽는 기능이다.**

현재 확인된 경로:

```text
휴대폰 KI → Win7 Master → 사내망 Win11 Slave
휴대폰 KI ← Win7 Master ← 현재 PC 상태 응답
```

v0.1.33에서 앱 재시작 없이 새 요청 5회와 답장 5회가 처리됐고, 사용자가 휴대폰에서 직접 송신·수신한 사실을 확인했다. 동일한 마우스 오버·입력·기본 왕복 시험은 추가로 반복하지 않는다. 다만 이는 현재의 읽기 전용 상태 조회 경로에 대한 판정이며, 장기 무인 운용과 시뮬레이션 진행률 기능까지 완성됐다는 뜻은 아니다.

사용자가 정한 다음 우선순위는 **실행 중인 프로그램 목록과 시뮬레이션의 현재 진행 상태 확인**이다. 원격 프로그램 실행·종료·파일 조작은 이번 다음 단계에 넣지 않는다. 기본 수집과 정형 상태 판정은 LLM 없이 구현하는 방향으로 잡는다.

## 2. 진행 과정 요약

| 구간 | 해결한 과제 | 현재 판정 |
|---|---|---|
| 초기 진단 | Win7 KI 객체 구조 확인, 짧은 코드, 개별 자기 대화창 제한 | 필요한 대상 탐색과 제약을 확보 |
| v0.1.21–23 | 실제 전송, 자동 입력, Send 위치 자동 탐색·커서 이동 | 사용자 휴대폰 수신 확인. 상시 Send 마우스 오버 불필요 |
| v0.1.24–28 | 휴대폰의 새 요청 관측과 답장 연결, 두 차례 연속 왕복 | 현장 송수신 확인 |
| v0.1.29–30 | 작업 종료 후 로그 스트림 해제, 의미 있는 PC 상태 답장 | 기능 구현 및 내부 검사. 사용자에게 별도 자체 검사 실행 요구하지 않음 |
| v0.1.31–32 | Win7 Master와 Win11 Slave 연결, Slave 상태를 휴대폰에 전달 | 실제 두 PC와 휴대폰의 전체 경로 확인 |
| v0.1.33 | 재시작 없는 반복 요청, 다음 코드 전달, 이전 코드 재사용 방지 | 새 요청·답장 5회 현장 확인 |

초기에는 접근성 API가 성공을 반환해도 KI가 실제로 메시지를 보내지 않는 경우가 있었다. 현재 전송은 검증된 입력 방식과 실제 마우스 클릭을 사용한다. 이 부분을 다시 다른 방식으로 교체하는 작업은 현재 목표에 필요하지 않다.

## 3. 최신 현장 결과와 판정의 근거

대상 파일:

- Master 로그: `remote-monitor-master-20260910-025326-pid10616.log` (사용자 로컬 자료, 저장소 미포함)
- Slave 로그: `remote-monitor-slave-20260910-025256-pid99784.log` (사용자 로컬 자료, 저장소 미포함)

Master의 `STATUS_REQUEST_COMPLETE`는 5회다. 대응하는 전송 5회 모두 `ACTION_RETURNED / NONE`, 입력 1회·클릭 1회, 전송 후 입력란 `EMPTY`로 기록됐다. 사용자가 새 M코드 5회 전송과 휴대폰 답장 5건을 직접 확인했으므로, API 반환뿐 아니라 실제 수신까지 확인된 결과다.

Slave의 `STATUS_SENT`는 6회로, 직접 조회 1회와 휴대폰 요청에 따른 조회 5회에 대응한다. 휴대폰 답장이 6건이었다는 뜻은 아니다. Master 전송 단계는 각 약 9–10초였으며, 이 수치를 전체 휴대폰 왕복 지연으로 해석하지 않는다.

다섯 번째 성공 이후 다음 요청을 기다리던 중 `STATUS_TARGET_CHANGED`로 종료됐다. 이 단계의 입력·클릭은 모두 0회이고 최종 `pending_write=False`다. 따라서 앞선 5건의 통신 실패가 아니라 대상 창 조건이 깨진 뒤 추가 동작을 중단한 기록이다. 창 닫기·다른 창 선택 등 정확한 실제 조작은 로그로 확정할 수 없다.

| 항목 | 확인 수준 |
|---|---|
| 모바일 요청 → Slave 조회 → 모바일 답장 5회 | 로그와 사용자 수신 확인으로 현장 검증 완료 |
| Send에 마우스를 미리 올려놓지 않는 자동 전송 | 이전 현장 확인 및 최신 전송 기록 |
| 이전 코드·중복 예약·취소·잘못된 연결 정보 거부 | 코드 및 기존 내부 검사. 이번 현장에서 모든 항목을 다시 시험했다는 뜻은 아님 |
| 작업 종료 후 로그 파일 해제 | 기존 내부 검사 통과. 이번 첨부만으로 앱을 켜 둔 채 전송했는지는 확정하지 않음 |
| 장시간 대기·대화 이력 누적·연결 단절·재시작 후 복구 | 현장 검증이 남음. 업무 중 장시간 시험은 요구하지 않음 |

현재 Master는 잠기지 않은 화면에서 개별 자기 대화창을 전경에 유지해야 한다. 창 이동·크기 변경이나 전송 중 입력 간섭은 운용을 중단시킨다. Send 위에 마우스를 계속 둘 필요는 없다. 이는 Master의 메신저 조작 제약이며, Slave의 업무 프로그램에 같은 마우스 위치 조건을 요구하는 것은 아니다.

## 4. 코드 검토 결과

현재 읽기 전용 상태 조회 범위에서는 이번 검토에서 새로 확인된 중대한 결함이 없다. 독립 코드 검토 결과도 같다. 다만 다음 기능을 붙일 때 바뀌어야 하는 지점은 명확하다.

1. **현재 Slave는 업무 프로그램을 관찰하지 않는다.** `MachineStatus`와 `PcStatusReport`가 전달하는 것은 시각, 부팅 후 경과 시간, 전체/가용 RAM, Remote Monitor 버전이다. 프로세스 목록·개별 CPU·작업명·진행률·완료 여부는 아직 없다.
2. **상태 응답 계약도 함께 확장해야 한다.** 현재 Slave 응답은 제한된 ASCII 128자 프로토콜이며, 휴대폰 답장은 정해진 형식과 180자 상한을 검사한다. 프로그램 목록을 문자열 뒤에 붙이거나 검증을 해제하는 식으로 구현하지 않는다. 필요한 필드와 길이를 정하고 양쪽 파서·정확한 답장 승인·검사를 함께 바꾼다.
3. **현재 전송 보호는 유지한다.** 개별 자기 대화창, 대상 창·입력란·Send 재확인, 정확한 입력 확인, 한 번의 클릭, 취소와 불확실한 전송 후 중단을 재사용한다. 목록이나 로그 내용에 M 요청 코드가 포함되더라도 자기 답장이 새 요청으로 인식되지 않도록 출력 규칙도 유지해야 한다.
4. **조회 결과와 실제 업무 완료를 구분한다.** 프로세스가 존재한다고 시뮬레이션이 진행 중이거나 성공한 것은 아니다. CPU가 낮다고 멈춘 것도 아니고, 프로세스가 사라졌다고 성공한 것도 아니다. 확인 불가와 정상 완료를 명확히 구별해야 한다.

관련 소스: [LinkTypes.cs](src/RemoteMonitorLink/LinkTypes.cs), [StatusTransport.cs](src/RemoteMonitorLink/StatusTransport.cs), [PcStatusReport.cs](src/RemoteMonitorMaster/PcStatusReport.cs), [StatusSession.cs](src/RemoteMonitorMaster/StatusSession.cs), [RoundTripTest.cs](src/RemoteMonitorMaster/RoundTripTest.cs).

Master↔Slave는 인증서 핀과 인증키를 확인하는 TLS 연결이다. 모바일 M코드는 화면에서 관측하는 요청 표식이며 발신자·본문을 암호학적으로 인증하는 수단은 아니다. 현재의 승인된 읽기 전용 조회 범위를 유지한다.

향후 별도로 변경 작업을 요청받는다면 실행 전에 Slave 측 요청 식별·중복 방지·결과 보관이 필요하다. 현재는 Slave 조회가 영구 M 예약보다 먼저 일어나며, 연결 요청에 작업 ID가 없으므로 조회 함수를 곧바로 프로그램 실행 함수로 바꾸면 안 된다. 이 작업은 지금 만들지 않는다.

## 5. 다음 개발 범위: LLM 없는 실행 상태 모니터

### 공통 기능 — 프로그램 종류와 무관하게 먼저 준비할 범위

- Slave에서 실행 중인 사용자 앱과 감시 대상으로 지정한 시뮬레이션 프로세스를 읽는다. 창이 없는 계산 프로세스도 누락하지 않도록 대상 프로그램 구조를 확인한다.
- 프로그램 이름, 인스턴스 구분, 실행 시작 후 경과 시간, 메모리, 짧은 구간의 CPU 사용량, 조회 시각을 보여 준다. 접근할 수 없는 값은 0이나 정상으로 대체하지 않고 `확인 불가`로 표시한다.
- 여러 프로세스를 쓰는 앱과 동시에 여러 작업을 실행한 경우를 구별한다. 프로세스 경과 시간은 시뮬레이션 작업의 경과 시간과 다를 수 있다.
- 휴대폰에는 업무 앱 위주의 짧은 요약을 보낸다. 목록이 길어지면 생략된 개수를 표시하며, 전체 목록을 확인할 수 있는 범위와 길이 제한을 함께 정한다. 수백 개의 시스템 프로세스나 원문 로그를 그대로 답장하지 않는다.
- 기본 전송 데이터에 계정명, 명령줄, 전체 파일 경로를 넣지 않는다. 현재 상태 확인에 필요한 이름과 수치부터 사용한다.

### 시뮬레이션별 진행 상태 — 실제 근거가 필요한 범위

| 알고 싶은 내용 | 우선 사용할 근거 | 근거가 없을 때 |
|---|---|---|
| 어떤 프로그램이 실행 중인가 | Windows 프로세스·앱 목록 | 접근 실패를 표시 |
| 얼마나 진행됐는가 | 프로그램이 기록한 진행률, 완료/전체 단계 수, 상태 API | `진행률 미제공` |
| 현재 무엇을 계산 중인가 | 현재 단계명·작업 상태 로그 | 프로세스 실행 상태만 표시 |
| 끝났는가 / 실패했는가 | 해당 작업의 명시적인 완료·오류 기록 또는 결과 상태 | 종료만 관측했다면 `종료됨, 결과 확인 불가` |
| 최근에도 상태가 갱신되는가 | 해당 작업의 최근 진행 기록과 조회 시각 | 갱신 시각을 표시하되 정지로 단정하지 않음 |

우선 해당 프로그램의 기존 상태 API 또는 로그를 읽고, 그것이 없다면 접근성 상태 표시를 검토한다. 정형 정보로 해결할 수 있는 범위에 LLM, 화면 해석 서버, 자연어 명령 계층을 추가하지 않는다. 실제 작업에 대응하는 최신 기록인지 확인하며 예전 작업의 완료 로그를 현재 작업 결과로 오인하지 않아야 한다.

진행률 구현에 추가로 필요한 정보는 **사용하는 시뮬레이션 프로그램 이름·버전과 진행 상태가 보이는 화면 또는 로그 예시**다. 공통 프로그램 목록 설계는 이 정보 없이도 진행할 수 있지만, 프로그램별 진행률 판정 규칙을 임의로 정하지 않는다.

### 최소 변경 위치

| 기존 위치 | 다음 변경 |
|---|---|
| Slave 및 공유 `MachineStatus` | 읽기 전용 프로그램 상태 수집과 누락/실패 표시 |
| `LinkTypes` / `StatusTransport` | 필요한 상태 필드, 제한된 응답 크기, 양쪽 형식 검증 |
| `PcStatusReport` / 전송 승인 검사 | 짧은 휴대폰 요약과 정확한 출력 승인 |
| Master / Slave 화면 | 수집·전송·완료 상태와 조회 시각 표시, 이름·버전 유지 |
| 기존 자체 검사 | 목록 변화·접근 실패·진행률 근거·출력 형식 회귀 검사 |

검증된 KI 입력·커서 이동·클릭 경로는 유지한다. 새 진단 프로그램, 범용 플러그인 구조, 원격 실행 엔진은 만들지 않는다.

## 6. 테스트를 통합하는 방법

개발 PC에서 먼저 목록 없음/여러 앱/여러 인스턴스, 수집 중 종료, 접근 불가, 오래된 로그, 완료·실패·진행률 미제공, 긴 목록과 잘못된 응답을 검사한다. 기존 연결·취소·중복·출력 승인 검사도 변경 부분에 맞춰 실행한다. CPU나 프로세스 종료만으로 완료를 판정하는 회귀가 없어야 한다.

사용자 현장 확인은 **실제 업무 앱을 실행해 둔 상태에서 한 번 준비하고, 한 요청의 휴대폰 답장으로 프로그램 목록·실제 진행 표시·응답 도착을 함께 대조하는 방식**으로 통합한다. 다음 상태 전환도 짧은 실제 작업으로 볼 수 있으면 같은 세션에서 확인하고, 이를 위해 긴 시뮬레이션을 별도로 실행하거나 기다리게 하지 않는다. 기본 PING, 동일 마우스 오버, 같은 두 차례·다섯 차례 왕복을 다시 독립 시험으로 요구하지 않는다.

장기 대기와 이력 누적은 기능이 안정된 뒤 사용자가 퇴근 시 설정할 수 있을 때 진행한다. 연결 단절과 재시작 복구는 그 동작을 구현·변경할 때 묶어 검증한다. 현장 사용자는 EXE만 실행하며 개발용 자체 검사 명령을 따로 실행하지 않는다.

## 7. 이번 준비 결과와 유지한 기준판

- 현재 통신 단계의 완료 판단, 남은 운용 경계, 다음 상태 조회 기능의 범위·코드 변경 지점·통합 시험 기준을 이 문서에 정리했다.
- 새 업무 기능은 아직 구현하지 않았다. 이번 작업은 진행 검토와 다음 개발 준비이며, EXE나 배포 ZIP을 다시 만들지 않았다.
- 기존 v0.1.33 검증 기록: 양쪽 Release 빌드 경고·오류 0, 실제 .NET 4.8 EXE 자체 검사 종료 코드 0, 패키지 파일 일치 확인. 문서만 추가하는 이번 검토에서는 빌드를 불필요하게 반복하지 않았다.
- 기준 ZIP: `dist/Remote-Monitor-v0.1.33-win7-win11-net48.zip` (170197 bytes).
- 이번 검토에서 다시 확인한 ZIP SHA-256: `EA8A7B834392BA681E2E2403F6F8362BE0A56F1CC9F3603B0A5233E752A9AF9C`.
- 기존 누적 소스 변경과 이전 배포물을 보존했다. 커밋·푸시·외부 배포는 하지 않았다.

## 후속 구현 — v0.1.34 (2026-09-10)

이 검토 이후 사용자의 다음 단계 진행 요청에 따라 공통 프로그램 상태 조회를 구현했다. Slave와 같은 세션의 이름·PID·CPU·RAM·프로세스 실행 시간을 수집하며, 로컬 표는 최대 128개, 휴대폰은 CPU 사용률 우선 최대 8개와 생략 개수를 표시한다. 진행률은 `미제공`이며 LLM·프로그램 제어는 추가하지 않았다.

양쪽 Release 빌드와 자체 검사, 실제 개발 PC 목록 수집, 추출 패키지 파일 일치와 화면 표시를 확인했다. 새로 길어진 KI 답장의 실제 Win7/모바일 배달 확인은 당시 WIN7-TEST의 M 요청 한 번으로 진행한다. [WIN7-TEST.md](WIN7-TEST.md)는 이후 갱신되었으며 이 문서의 앞부분은 v0.1.33 검토 시점 기록으로 유지한다.

## 후속 통합 — v0.1.35 (2026-09-10)

첨부된 v0.1.34 Master `044041-pid11452`, Slave `044015-pid104268` 로그에서 상태 답장 두 번과 각 전송 후 입력란 비움을 확인했다. Slave의 조회 3회는 직접 조회 1회와 M 요청 2회에 대응한다. 두 번째 완료 후 `STATUS_TARGET_CHANGED`로 다음 대기가 중단됐고 추가 입력은 없었다. 사용자는 M 입력 2회를 알렸으며, 이번 로그만으로 휴대폰의 실제 배달과 수치 일치까지 확인됐다고 확대하지 않는다.

사용자 요청에 따라 기존 프로그램 상태 시험에 고정 평문 명령 인식을 합쳤다. `total status`가 기존 전체 상태·프로세스 수치를 조회하고 `pwrsi`는 제한 전 PowerSI 이름 필터로 같은 상태 수치를 조회한다. `help`와 지원하는 `help [명령어]`는 연결 없이 목록·설명을 제공한다. 같은 세션에서 상태 대조와 도움말·반복 명령 인식을 함께 확인하며 별도의 기본 M 왕복이나 마우스 오버를 요구하지 않는다.

기존 메신저 전송·취소·정확한 출력 승인 경로를 재사용한다. 평문 모드는 모든 노출된 history Text의 기존 순서를 보존한 추가 부분만 검사하고, 두 번 관측과 전송 직전 재관측을 수행한다. 이력 잘림·재배열·동시 명령은 거부하며, 답장은 대문자여서 소문자 명령과 구별한다. 완전한 메시지/발신자 인증이나 시뮬레이션 진행률 기능으로 해석하지 않는다. Slave 표에는 전체/POWERSI·PWRSI 전용 조회 범위를 구별해 표시한다.

당시 현장 안내는 [WIN7-TEST.md](WIN7-TEST.md)로 갱신했다. 앞의 v0.1.33~34 절은 해당 시점 기록이다. 내부 검증과 패키지 일치 결과는 로컬 `dist/REVIEW-CHECKPOINT-v0.1.35-plain-commands.md`에 기록했다.

## 후속 수정 — v0.1.36 (2026-09-10)

v0.1.35의 Master `052436-pid1340` 원본은 `(2)` 파일의 정확한 바이트 접두부이다. 첫 세션은 HELP 1회 전송 후, 두 번째 세션은 전송 0회에서 `RECEIVE_HISTORY_PREFIX_CHANGED`로 중단했다. Slave 로그의 상태 수집 1회는 Master 직접 조회이며, 휴대폰 `pwrsi`에 대한 Slave 응답 근거는 없다. 연결 장애나 사용자의 입력 실수로 단정하지 않는다.

평문 모드가 모든 Text를 평탄한 고정 이력으로 취급하는 구조를 수정했다. 상세 스냅샷의 메시지 행에는 첫 내용 Text 외 시간 Text가 있으며, 기존 내용·행·경로는 엄격히 비교하면서 같은 부모의 후속 시간 표시 1개만 분리한다. 폴링 로그에는 실제 차이를 특정할 메타데이터가 없어 시간 변화가 이번 실패의 정확한 원인이라고 확정하지 않는다. 후속 실패에는 최초 차이의 행·형태·경로 일치 여부를 원문 없이 기록한다.

사용자가 제공한 PowerSI II 25.1.0.09191.616638 화면에서 주요 근거는 Output 로그이며, 하단 상태는 보조 정보다. 도킹 패널의 위치·크기는 고정하지 않는다. 직접 읽기 우선으로 마지막 AFS 주파수·중단/재개 이벤트·메모리 경고 기록·상태 표시를 요약하며, 주파수나 CPU를 진행률로 환산하지 않는다. 공급자 호출 정지는 제한된 별도 읽기 작업에서 격리한다. 화면 예시의 마지막 주파수 38.000 MHz와 과거 중단·재개 기록은 파서 검사 자료이며 현재 Slave 실측 결과로 표시하지 않는다.

현장 확인은 한 세션에서 `pwrsi → total status → pwrsi`로 묶었다. Output 추출·전체 수치·명령 연속/반복·실제 모바일 수신을 함께 확인한다. 개발 PC에는 해당 PowerSI 프로세스가 없어 실제 객체 노출 여부는 새 패키지의 현장 로그로 확인해야 한다. LLM·외부 화면 전송·원격 실행은 구현하지 않는다. 검증·패키지 근거는 v0.1.36 체크포인트에 추가한다.

## 후속 수정 — v0.1.37 (2026-09-10)

v0.1.36 Master `061817-pid24156`의 두 세션 모두 `RECEIVE_HISTORY_CONTENT_CHANGED`에서 전송 0회로 중단했다. 첫 번째는 기존 64행 중 0행의 날짜 형태가 파일명 형태로 바뀌었고, 두 번째는 39행의 Text 이름이 달라졌다. 행 루트 비교는 해당 지점까지 통과했다. Slave `061804-pid102036`에는 직접 전체 상태 조회 1회만 있으며 PowerSI 수집 요청은 없다. 따라서 이번 기록은 PowerSI 추출 실패를 입증하지 않는다. 이전 내용의 실제 변경 원인을 가상화나 렌더링으로 확정하지 않는다.

평문 수신 검사에서 모든 기존 행의 객체·경로·순서는 계속 보존하고, 본문 비교는 이전 스냅샷의 최근 3행으로 한정했다. 그보다 오래된 내용·시간 표시 변화는 새 요청이 될 수 없으며, 새로 추가된 행만 명령 후보로 검사한다. 최근 행 변경, 행 삭제·재배열·객체 변경, 중복 명령, 두 번 관측 및 전송 직전 검사는 유지한다. 64행 회귀 검사에 실제 차이 위치, 과거 행의 명령어 변형, 이전 최근 3행 경계와 여러 새 행 추가를 포함했다. 완전한 메시지 인증으로 해석하지 않으며 승인된 읽기 전용 조회 범위를 유지한다.

Slave에 `PowerSI 확인` 버튼을 추가해 같은 필터·별도 읽기 작업·요약 표시·관측 로그를 재사용한다. 수신 시작이나 모바일 명령 없이 실행 가능하며 Stop으로 취소한다. 뒤늦은 로컬 결과는 새 Master 결과를 덮어쓰지 않는다. 현장 시험은 기존 한 세션 통합 순서를 유지하되, 메신저가 중단되면 재시작 반복 대신 로컬 확인 한 번으로 PowerSI 객체 노출 여부를 분리한다. 모바일 결과가 정상이라면 별도의 로컬 확인은 요구하지 않는다. 실제 PowerSI 읽기 성공 여부는 아직 현장 확인 대상이다.

## 후속 확인·보완 — v0.1.38 (2026-09-10)

정정 첨부한 Master `064845-pid21028`은 v0.1.37이다. 15:49:27 READY 뒤 기존 66행에서 67행으로 증가했지만 첫 새 문구는 후보로 인식되지 않았다. 전송 직전 상세 기록의 해당 행(0부터 센 66행, Text259)은 길이 6, 활성·화면 표시 상태였고, 과거 여러 행과 같은 이름 지문이었다. `pwrsi`는 길이 5이므로 관측된 문구는 정확히 일치하지 않는다. 이것은 기존 동일 문구를 중복 제거한 경우가 아니다. 원문이 없으므로 추가 문자가 공백인지 철자인지는 확정하지 않는다. 마지막 3행 비교가 해당 새 행을 계속 보호했다.

15:50:26 `TOTAL_STATUS`가 인식됐고 15:50:39 답장 1회 입력·클릭 후 입력란 비움을 확인했다. 이후 다음 대기에서 `STATUS_TARGET_CHANGED`로 종료했으며 추가 전송은 없다. Slave `064823-pid104904`의 STATUS 2회는 직접 조회 1회와 해당 명령 1회에 대응한다. 양쪽 시계가 약 7~9초 어긋나 절대 시각만으로 직접 대응시키지 않는다. 로컬 PowerSI 확인은 첫 회 Stop으로 취소, 두 번째는 약 4.1초 뒤 TIMEOUT이다. 실제 Output 직접 읽기는 아직 성공 근거가 없다.

명령 원문 최대 64자 범위에서 앞뒤 일반 공백·NBSP만 제거한 허용 명령 ID를 별도 저장한다. 원래 이름 길이·해시·객체·경로는 바꾸지 않고, 새 Name 읽기가 기존 해시와 맞을 때만 ID를 만들며 조회와 전송 직전에 같은 원문을 재확인한다. 대소문자·내부 공백·철자·개행·제로 폭 문자를 보정하지 않는다. 같은 문구의 새 행, 공백 있는 반복 명령, 원문 변경과 보이지 않는 중복 명령을 회귀 검사에 포함한다. 원문을 남기지 않는 글자 수·분류 로그와 로컬 미인식 표시를 추가한다. 공백 보완이 이번 6자 문구를 반드시 처리한다고 보장하지 않는다.

PowerSI는 기존 약 4초 제한을 유지하면서 개별 객체의 기본 속성 조회를 묶고, 마지막 읽기 단계·제한된 노드 수를 로컬 로그로 전달한다. 화면·설계명·Output 원문을 추가로 기록하거나 전송하지 않는다. 다음 현장 확인은 이미 완료된 `total status`를 다시 요구하지 않고 `pwrsi` 답장 확인 후 `pwrsi` 새 메시지 1회로 통합한다. 원격 명령이 막혔을 때만 기존 로컬 PowerSI 확인 버튼을 한 번 사용한다.

## 후속 수정 — v0.1.39 (2026-09-10)

v0.1.38 Master `075803-pid14288`는 새 `pwrsi` 5자 EXACT를 인식하고 답장 316자 전송 절차와 입력란 비움을 1회 완료했다. 두 번째 대기는 `STATUS_TARGET_CHANGED`에서 종료했다. 기존 창 소멸/전경 HWND 불일치 검사이며 사용자 클릭이나 두 번째 명령 전송 여부까지 입증하지 않는다. 모바일 도착은 로그만으로 확정하지 않는다. Slave `075800-pid45136`는 원격 PowerSI 요청에 약 3.558초 뒤 TIMEOUT을 반환한 다음 STATUS_SENT를 기록했다. 마지막 체크포인트 `P1|WALK|320`은 최소 그 수까지 탐색했다는 뜻이지 320번째 객체가 고장 났다는 뜻은 아니다. 두 로컬 확인 실패는 UI_STOP/LISTEN_STOPPED 직후여서 자동 시간 초과로 합산하지 않는다.

기존 PowerSI 읽기는 Output에 접근하기 전 전체 Raw UI 트리를 끝까지 순회했다. v0.1.39는 도킹 구조를 찾을 때 Tree/List/DataGrid/Table 등 목록 내부와 텍스트 문서를 확장하지 않고, 발견한 Output·StatusBar 영역만 다시 전체 읽는다. Output이 이름 없는 목록 내부에만 숨은 구조는 지원하지 않는다. 선택 영역과 발견 과정은 총 1536개 제한을 공유하고 절대 깊이 20·기존 worker 3500ms 제한을 유지한다. Output 표식/부모와 복수 영역 거부는 보존한다. 상태줄 내용은 조회 중 바뀔 수 있으므로 해당 이름 자체를 정체성으로 비교하지 않고 같은 요소·PID·타입을 유지한다. 제한된 로그에 DISCOVER/REGION/OUTPUT_READ/STATUS_READ 단계를 추가했다.

독립 리뷰와 실제 UIA 시험창으로 1000개 네트 행 건너뛰기, label보다 먼저 나오는 이름 없는 Output 목록의 재탐색, 갱신된 상태줄 이름 읽기, 객체/깊이 경계를 확인했다. `scripts/test-powersi-discovery.ps1`은 개발용이며 사용자에게 실행을 요구하지 않는다. 실제 PowerSI 제공자의 성능/텍스트 노출은 검증되지 않았다. 현장 확인은 Slave의 PowerSI 확인 버튼 한 번만 수행하고 Slave 로그만 받는다. Master·휴대폰·동일 명령 반복/기본 통신 시험을 다시 요구하지 않는다. 공백 안내는 pwrsi에는 공백 불필요, total status는 단어 사이 한 칸, 앞뒤 공백은 자동 제거라는 표현으로 정정했다.

## 후속 수정 — v0.1.40 (2026-09-11)

v0.1.39 Slave `233124-pid98188`의 원격 PowerSI 읽기는 약 1.243초 뒤 `OUTPUT_AMBIGUOUS`, `P1|OUTPUT|2`를 반환했다. 시간 초과가 아니라 후보 두 개에서 내용을 읽기 전에 중단한 것이다. 실제 후보가 탭과 본문인지는 로그에 없어 확정하지 않는다. Master `233109-pid15120`는 새 12자 지원 외 문구와 5자 EXACT 명령을 관측하고 PWRSI 답장 전송 절차 1회, 입력란 비움까지 기록했다. 다음 대기는 STATUS_TARGET_CHANGED로 종료했다. 휴대폰 배달·두 번째 동일 명령 완료는 로그만으로 확대하지 않는다. 첨부 Slave에는 로컬 확인 실행이 없지만 원격 요청도 같은 읽기 코드를 사용하므로 이번 원인 확인에 유효하다.

Output 후보를 최대 4개까지 기존 총 1536객체·깊이20·worker3500ms 제한 안에서 읽는다. 실제 텍스트를 가진 영역이 하나이고 나머지가 완전 탐색된 제목 전용 구조일 때만 선택한다. 실제 텍스트가 둘이면 같아도 합치지 않으며, 다른 후보가 읽기 불가/불투명한 컨트롤이면 제목이라고 추정하지 않는다. 탭은 본문 Pane을 가질 수 있으므로 부모 타입만 보고 제외하지 않는다. 후보 선택이 불명확하면 내용/제목/불명 수를 고정 형식 CHOICE 진단으로 남긴다.

Text/Value 패턴이 제목 Output만 반환했는데도 전체 하위 영역을 읽은 것으로 간주하던 버그를 함께 수정했다. 실제 비제목 텍스트를 채택했을 때만 하위 중복 읽기를 생략한다. 제목 대소문자 비교는 기존 발견·텍스트 제거와 동일하게 유지한다. 원문·이미지 외부 전송이나 LLM, 새 입력 조작, 제한 시간 연장은 추가하지 않았다. 현장 시험은 여전히 Slave의 PowerSI 확인 한 번이며 README 맨 앞과 Slave 최초 화면에 통신 준비 불필요 안내를 배치했다.

## 후속 전환 — v0.1.42 (2026-09-11)

v0.1.40 Slave `001218-pid104664`는 로컬 확인 약1.723초 뒤 OUTPUT_AMBIGUOUS, `P1|CHOICE|1|0|1`로 완료했다. 이것은 실제 Output 본문을 확실하게 식별했다는 증거가 아니다. v0.1.41 객체 진단판은 중간 산출물로 보류했으며 사용자에게 반복 현장 시험을 요구하지 않았다.

사용자가 최종 운영은 PowerSI와 같은 워크스테이션의 LM Studio/Local LLM이어야 하고 모델은 교체 가능하다고 명시했다. 이에 PowerSI 조회를 창 캡처 → 127.0.0.1의 로드된 이미지 모델 → 정해진 텍스트 요약으로 전환했다. 모델명은 고정하지 않고 LM Studio의 이미지 지원/로드된 인스턴스 정보를 확인한다. 모델 다운로드·로드·언로드, 외부 URL·프록시·리다이렉트·클라우드 대체 경로는 추가하지 않았다. LM Studio 자체는 해당 워크스테이션의 로컬 모델로 운용하며 원격 모델 중계는 사용하지 않는 조건이다.

이미지와 모델이 옮긴 Output/상태 원문은 Slave 메모리 및 비교 화면에만 두고 프로토콜·진단 로그에서 제외한다. Master에는 기존 프로세스 수치와 고정 상태·주파수·경고 여부·캡처 UTC 시각을 PS2로 전달한다. MODEL_READ로 모델 판독임을 구분하며 진행률/완료를 추정하거나 판독문을 명령으로 실행하지 않는다. 창 캡처는 별도 제한 시간 작업이며 활성화·입력 조작이나 전체 데스크톱 캡처 대체 경로가 없다.

양쪽 Release 빌드는 경고/오류0, 실제 EXE 자체 검사 종료 코드0, 추출 ZIP의6개 파일은 빌드 출력·문서와 바이트 일치했다. 로컬 모의 HTTP로 모델 교체·이미지/응답 제한·인증·리다이렉트·취소·시간 초과와 구조화 응답을 검사했다. 최종 추출 Slave의 자체 시험창 캡처는320x180, 텍스트890픽셀, PNG1783바이트, 약70ms였고 이미지 파일 저장/전경 변경이 없었다. 이 시험창 성공은 실제 Cadence 패널 렌더링이나 사용자의 Gemma 판독 정확도를 입증하지 않는다. 개발 PC에는 해당 PowerSI/LM Studio 서버가 없어 현장 확인 대상으로 남겼다.

새 배포 ZIP은 `dist/Remote-Monitor-v0.1.42-win7-win11-net48.zip`, 292405바이트, SHA256 `4A23D29B8A5FA7C63D00A238DD3CF347145D5EF766959C2FE57EE55B02A46101`이다. 기존 v0.1.40 ZIP의 해시는 유지됐다. 커밋·푸시·외부 게시 없이 누적 변경과 기존 배포물을 보존했다. 현장 확인은 LM Studio 설정 후 휴대폰 `pwrsi` 한 번으로 캡처·로컬 판독·기존 모바일 답장을 합쳐 확인한다. 실제 이미지와 판독 원문은 Slave에서 비교하며 외부 첨부를 요구하지 않는다. M/help/total status/마우스 오버/장시간 대기는 재시험하지 않는다.

## Slave 판독 집중 — v0.1.43 (2026-09-11)

추가 Slave 로그 `011929-pid109780 (2)`는 이전 파일의 정확한 바이트 접두부에 실행 기록을 추가한 파일이다. 마지막 로컬 실행3회는23.663/58.168/21.110초 뒤 모두 `VISION_INVALID_RESPONSE`, 상세 `INVALID_RESPONSE`로 완료했다. 앞선 MODEL_NOT_READY와 구별되지만, 기존 단일 오류 코드로는 응답의 어느 검사에서 실패했는지 알 수 없다. 사용자는 Output 크기를 바꿨다고 설명했으며, 이 로그만으로 크기 변경이 실패 원인이라고 단정하거나 실제 OCR 성공/실패를 판정하지 않는다.

응답 파서가 빈 `tool_calls: []`와 빈 `refusal: ""`까지 거부하는 호환성 결함을 수정했다. 실제/잘못된 도구 호출과 비어 있지 않은 거부, 잘못된 역할/완료 상태, JSON/필드/길이/일치 오류는 계속 거부한다. 실패 코드를 목록·HTTP·바깥 응답·본문·Output/status 필드 단계로 구분한다. 거부된 응답은 최대40000자의 제어문자 정리된 로컬 미리보기로만 보관하고 UNVALIDATED 표시한다. 시뮬레이션 상태로 채택하거나 로그/프로토콜로 보내지 않는다. 요청 스키마·모델·이미지·제한 시간·자동 재시도 정책은 바꾸지 않았다. 수정한 빈 목록 결함이 이번 현장 실패의 원인이라는 근거는 아직 없다.

사용자의 새 지시에 따라 앞으로 현장 확인은 Slave 화면 인식/정보 추출에 집중하며 Master·메신저 시험은 제외한다. `SLAVE-TEST.md`와 시작 화면에 PowerSI 확인 한 번, Slave 시작/IP/연결파일 불필요를 명시했다. Master 버전 메타데이터는 공유 버전에 맞췄지만 이번 빌드/배포 대상은 Slave뿐이다. 향후 통신을 재개할 때 버전을 맞추며 현재는 기존 Master를 교체하거나 연결하지 않는다.

Slave Release 경고/오류0, 실제 EXE 자체 검사 종료0, stderr0바이트. 모의 HTTP의 빈 메타데이터 허용·실제/잘못된 동작 거부·상세 코드·미리보기 비밀문자열의 예외/로그/프로토콜 제외·재시도 없음과 기존 경계 검사를 통과했다. 독립 리뷰에서 차단 결함은 없었고, 추출된4개 파일은 출력/안내문과 일치했다. Slave 화면의 버전/안내 표시도 확인했다. 실제 PowerSI/Gemma 응답은 아직 현장 확인 대상이다.

Slave 전용 ZIP `dist/Remote-Monitor-Slave-v0.1.43-win11-net48.zip`:89182바이트, SHA256 `7488C60B3CE95A5B7C450C104435C1BB00110352C1CCE5E0339EC0ACE0DB4BA0`. 이전 v0.1.42 ZIP을 보존했다. 별도 모델 추천 작업도 사용자 요청으로 생성했으며 RTX A6000/LM Studio의 경량 OCR·버튼 위치 인식 후보를 공식 출처 중심으로 조사한다. 이 작업에서 모델 다운로드/변경이나 외부 화면 업로드는 하지 않는다.

## Output 로그 전사로 목표 정리 — v0.1.44 (2026-09-11)

사용자는 PowerSI가 진행률을 제공하지 않으며 Output 로그를 직접 보고 진행 상황을 판단한다고 명확히 했다. 목표를 최근 로그 일부의 정확한 전사로 맞췄다. 상태줄 판독·주파수/경고/완료 추론과 모델 본문의 필수 JSON 생성을 현재 비전 경로에서 제거했다. 기존 UIA 판독 코드는 이 변경의 호출 경로가 아니며 별도로 유지한다.

새 Slave 로그 `022456-pid110396`의 첫 로컬 실행은 약2.978초 뒤 CONNECTION_FAILED, 두 번째는 약22.076초 뒤 CONTENT_JSON_INVALID였다. 두 번째는 바깥 HTTP 응답/역할/완료 검사를 통과한 텍스트 본문을 JSON 객체로 읽지 못했다는 뜻이다. 실제 본문은 로그에 없으므로 평문이 정확했는지, 코드 펜스나 다른 형식이었는지는 단정하지 않는다.

v0.1.44는 보이는 Output의 최근 최대12줄·2000자를 평문으로 요청한다. 단어·숫자·단위·보이는 완료 문구를 그대로 옮기고 진행률이나 완료 상태를 생성하지 않도록 지시한다. 긴 응답은 마지막 부분을 남기며 생략을 표시한다. OUTPUT_READ는 모델이 텍스트를 반환했다는 고정 결과일 뿐 정확한 OCR이나 완료 판정이 아니다. Slave 본창과 기존 이미지/텍스트 비교 창에서 전사본을 확인한다. 현재는 원문을 Slave 메모리에만 유지하며 메신저 발췌문 전달 연결은 이후 단계다.

127.0.0.1 전용·프록시/리다이렉트 금지·단일 요청·도구 호출 거부·HTTP 바깥 구조/완료 검사·본문 크기/제어문자 제한·취소/기한은 유지한다. 모델 교체나 로드, 원문/이미지 파일 저장, Master 현장 재시험은 하지 않았다. 별도 모델 추천 작업에도 진행률 추론보다 정확한 소문자·숫자·단위 OCR을 우선하도록 새 요구를 전달했다.

Slave Release 경고0/오류0, 실제 EXE 자체 검사 종료0·stderr0바이트. 평문/완료 문구 보존, 마지막12줄·2000자·유니코드 경계, unreadable 표식, 로그/프로토콜 원문 제외와 기존 모의 HTTP 경계 검사를 통과했다. 독립 리뷰의 경고 부재 오해 소지를 수정하여 OUTPUT_READ 요약은 NOT INTERPRETED로 표시한다. 추출4개 파일의 해시가 출력/안내문과 일치하고 FileVersion0.1.44.0 및 합성 로그의 오프스크린 표시를 확인했다. 실제 PowerSI/사용자 모델의 OCR 정확도는 아직 검증되지 않았다.

최종 ZIP `dist/Remote-Monitor-Slave-v0.1.44-win11-net48.zip`:87614바이트, SHA256 `48C60A25CCB0F8C14FFF1D19921D336B8D83E74F42C8EBAE8FEA3FA7EF51938B`. v0.1.43 ZIP 해시를 보존했다. 이번 검증/배포는 Slave만이며, 누적 작업을 커밋·푸시·외부 게시하지 않았다. 현장 확인은 기존 모델/Output 크기 그대로 PowerSI 확인 한 번 후 문구·숫자·순서를 비교하고 Slave 로그만 첨부한다.

## 두 수집 경로 비교 — v0.1.45 (2026-09-11)

v0.1.44 Slave `024925-pid109492`와 추가 기록본 `(2)`의 두 실행은11.868/6.559초 뒤 OUTPUT_READ였다. 사용자 설명과 두 화면은 Output 크기가 달라도 모델이 답변을 반환했다는 증거이며, 전사본은 첨부되지 않아 문자 정확도까지 확인한 것은 아니다. 사용자는 Output 클릭 후 Ctrl+A/C로 전체 텍스트가 복사된다고 확인했다. 전체 버퍼 원문 확보와 LLM 판독을 함께 비교하도록 목표를 확장했다.

새 `두 방식 비교` 버튼은 기존 LLM 화면 판독과 전체 텍스트 수집을 병행한다. 객체 식별은 native Output 캡션, 없으면 같은 실행의 UIA 이름→non-root HWND 연계를 확인하고 유일한 표준 multiline Edit/RichEdit만 직독한다. 직접 수집이 지원되지 않으면 같은 실행에서30초 동안 사용자의 Output Ctrl+A/C 새 복사본을 받는다. 앱이 마우스/키를 조작하거나 클립보드를 덮어쓰지는 않는다. 새 sequence, PowerSI 소유 PID·시작시각·세션을 검사하며 수동 선택 범위는 사용자가 대조해야 한다고 구분한다.

표준 컨트롤은8Mi UTF-16문자 상한, RichEdit의64K 초과 WM_GETTEXT 제한은 명시적으로 실패한다. 숨김/비밀번호/단일행/모호성/읽는 중 길이 변화/초과를 성공으로 처리하지 않는다. 원문은 로컬 메모리/비교 창에만 유지한다. main은 끝부분4000자, 비교 창 왼쪽은 전체 버퍼, 오른쪽은 캡처와 LLM 전사본이다. LLM에는 전체 텍스트를 보내지 않고 캡처만 보낸다. 수집 완료 시각과 LLM 캡처 시각을 구별하며 원자적 동일 시점이라고 주장하지 않는다.

직독 worker10초, 수동 복사 수신 worker3초, 비교100초 제한이다. 클립보드 지연 렌더링을 UI에서 제거해 Stop이 응답하도록 했다. 독립 리뷰에서 발견한 취소 후 로그 race, 동일 한도 초과 시 불필요한 복사 안내, LLM 완료 전 비교창 열기, 새 clipboard 일시 경합 문제를 수정했다. 같은 새 복사값 읽기는 최대3회 재확인하지만 키 입력/복사 명령을 재시도하지 않는다.

사용자의 추가 방향에 따라 최종 대상은 복수 PowerSI 인스턴스와 Ansys 등 여러 프로그램임을 문서에 명시했다. 단일 창 제한은 현장 수집 방식 확인을 위한 임시 조건이다. 프로세스 인스턴스 식별과 텍스트/화면 수집·표시를 구분하며 아직 범용 플러그인/자동 조작 틀을 추가하지 않았다. LLM 위치 추론 후 자동 클릭/복사는 향후 비표준 객체 후보로 남겼고, 검증되지 않은 좌표를 현재 클릭하지 않는다.

최종 Slave Release 경고0/오류0, 실제 EXE 자체검사 종료0·stderr0바이트. 표준 owned HWND의81,600자/2,401줄 읽기, 모호성/숨김/비밀번호/크기 검사, 전경·클립보드 무변경, IPC 원문/빈값/실패null 보존, 로그 원문 제외, 실제 비교 UI의 양쪽 텍스트/배치와 대기 중 버튼 차단을 확인했다. 추출4개 파일과 빌드/문서 해시가 일치하고 FileVersion0.1.45.0 및 실제 비교창의 오프스크린 합성 화면을 확인했다. 실제 PowerSI 컨트롤 지원과 사용자 수동복사 경로는 아직 현장 검증 대상이다.

배포 ZIP `dist/Remote-Monitor-Slave-v0.1.45-win11-net48.zip`:100304바이트, SHA256 `0FA1D253D22D9A710C8A73553D63DC48BE17317F78E085D6E629D6984C612D2D`. v0.1.44 ZIP 해시 보존, 누적 작업 보존, 커밋/푸시/외부 게시 없음. 현장 확인은 Slave에서 기존 모델/한 가지 Output 크기로 두 방식 비교 한 번만 수행하며 Master 재시험은 없다.

## Output 영역과 전사 분리 — v0.1.46 (2026-09-11)

v0.1.45에서 사용자가 전체 복사본은 정확하고 LLM 전사본은 원문과 전혀 다르다고 확인했다. 진행 중 로그 갱신 몇 줄로 설명할 수 없는 실패다. 오른쪽 이미지가 Output을 포함한 정상 PowerSI 전체 화면임도 확인했으므로, 전체 이미지에서의 영역 선택과 문자 전사를 나눠 확인한다.

두 방식 비교 한 번으로 전체 텍스트 수집과 전체 클라이언트 캡처→LLM Output 좌표→프로그램 픽셀 크롭→같은 모델의 크롭 문자 전사를 실행한다. 크롭은 재촬영·확대·생성이 아니며 동일 원본에서 잘라낸 픽셀이다. 영역 좌표는 정규화 범위/형식/방향/전체 이미지 여부를 검사하고 ASCII 공백·탭 차이는 허용한다. 미확정 시 OCR을 진행하지 않는다. 유효 좌표라도 Output 위치가 정확한지는 사람이 확인한다. OCR 프롬프트에는 문자·숫자·단위·순서를 그대로 전사하고 요약·의역·수정·추측하지 말라고 명시했다. 프롬프트 준수/정확도는 아직 현장 미검증이다.

비교 화면 기본 이미지는 크롭이며 선택 상자에서 전체 원본으로 전환한다. 위치 실패는 전체 원본만, OCR 실패는 이미 생성된 크롭도 유지해 보여준다. 전체 텍스트와 캡처는 시각이 다를 수 있지만 크롭과 그 전사본은 같은 정지 이미지이므로 그 불일치를 시뮬레이션 갱신으로 설명하지 않는다. LLM 기본60초는 단계별이며 전체 판독100초, 비교 실행105초 제한으로 오류 결과 반환 여유를 뒀다. 자동 입력·시뮬레이션 제어·Master 재시험은 추가하지 않았다.

Slave Release 경고0/오류0, 실제 EXE 자체 검사 종료0·stderr0바이트. 좌표/공백/픽셀 매핑/원본 픽셀 보존/두 요청의 입력 이미지 구분/모델 고정/로그·프로토콜 원문 제외/비교 UI 크롭·원본 전환 검사를 통과했다. 추출4개 파일의 원본 해시 일치, FileVersion0.1.46.0, 실제 비교창 오프스크린 합성 표시를 확인했다. 독립 리뷰 후 전송 전 실패에서도 사용할 수 있도록 이미지 선택 명칭을 '입력'으로 구분했다. 실제 모델의 영역 식별·전사 정확도 개선은 주장하지 않는다.

ZIP `dist/Remote-Monitor-Slave-v0.1.46-win11-net48.zip`:107805바이트, SHA256 `82F07D3BA3D6D1641C0E98781FA9EA6F6A2E090057DB1E94BC1F7556D13B0A22`. v0.1.45 ZIP과 누적 변경을 보존했다. 상세 검증은 `dist/REVIEW-CHECKPOINT-v0.1.46-crop.md`. 현장 시험은 기존 모델/크기로 두 방식 비교 한 번 후 크롭 영역과 문자 그대로 전사 여부 두 항목만 확인한다.

## Output 본문 경계와 동일 화면 모델 비교 — v0.1.47 (2026-09-11)

현장 v0.1.46은 전체 복사94,425자/1,616줄을 확보했지만 LLM 제안 상자가 본문 하단을 누락하고 오른쪽 패널을 포함했다. 유효한 좌표만으로 정확한 Output 크롭으로 취급하지 않도록, 제안 영역과 원본의 평평한 중립색 배경 경계를 대조하는 임시 보정을 추가했다. 제공된3개 화면의 전체 본문 경계와 하단 픽셀 보존을 실제 배포 EXE로 확인했다. 다른 테마/프로그램의 범용 식별이나 숫자 정확도가 검증됐다는 뜻은 아니다.

E4B로 두 방식 비교 한 번 후31B로 같은 화면 재판독한다. 새 캡처·재복사 없이 같은 PNG/전체 텍스트를 유지하고 최근8회 결과를 선택해 본다. 전체 본문과 실제 OCR 입력(하단 최대256픽셀 행, 폭에 따라2배 픽셀 복제)을 구분한다. VISION_RUN에 서버가 제공한 모델ID/이름/키/양자화·전체/OCR 해시·좌표·소요시간을 기록하며 원문/이미지/응답/토큰은 여전히 제외한다. 전체 이미지 해시뿐 아니라 실제 OCR 해시도 같아야 숫자 판독 조건이 같다. 31B 실패만으로 프롬프트 원인이라고 단정하지 않는다.

최종 Slave Release 경고0/오류0, 실제 EXE 자체검사 PASS·stderr0. 모의 로컬 서버에 대한 두 모델 동일 이미지 파이프라인, 로그 메타데이터/원문 제외, 재판독 취소·이력·늦은 콜백, 실제 비교 UI 및 추출4파일 해시를 확인했다. 실제 LM Studio 모델 정확도는 미검증이며 다음 현장 비교 대상이다. Master/모바일 반복 시험과 자동 클릭은 추가하지 않았다.

ZIP `dist/Remote-Monitor-Slave-v0.1.47-win11-net48.zip`:118556바이트, SHA256 `8D984A18D89A0E448B086A2030FF598F02EB452D60B29DB73BB9737F84BB1E95`. 상세 검증: `dist/REVIEW-CHECKPOINT-v0.1.47-model-compare.md`. 이전 v0.1.46 ZIP은 최종 점검 시 로컬 dist에 없어 보존 해시를 재확인하지 못했으며, 이번 작업에서 이전 ZIP 삭제·재생성은 하지 않았다. 커밋·푸시·외부 게시 없음.
