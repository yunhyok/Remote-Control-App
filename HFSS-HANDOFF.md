# Remote Monitor — HFSS 새 task 인계

기준: 2026-09-14. 전체 최신 상태/배포 검증은 [HANDOFF.md](HANDOFF.md), 작업 규칙은 [AGENTS.md](AGENTS.md)다. 이 파일은 긴 대화 없이 다음 작업을 시작하기 위한 요약이다.

인계 코드 기준은 `de64739b87dd3744b36872fc77ffb7b5d2ac1c36` / `v0.1.58-rc1`이다. 양쪽 Windows 빌드/EXE 자체 검사, CI34821919901, 공개 배포 ZIP/해시/버전 검증을 완료했다. 이후 인계 브랜치의 문서 전용 커밋은 검증 기록 갱신이며 배포 소스와 구분한다. 원본 task에서 패키지 검증을 완료했으므로 새 task에서 인계 확인을 위해 같은 빌드를 반복할 필요는 없다.

## 지금 할 일

**HFSS 구현은 아직 시작하지 않는다.** 사용자는 HFSS 버전·화면 구조·필요 정보를 별도로 설명하겠다고 했다. 새 task는 현재 코드/문서를 파악하고 사용자 설명을 기다린다. 동일 질문, PowerSI 반복 시험, 임의 HFSS 창/진행률 가정은 하지 않는다.

기준 브랜치는 `codex/slave-v0.1.51-guarded-copy`, PR #3이다. main은 이전 버전일 수 있다. 새 작업 트리에서는 이 브랜치의 최신 커밋과 LinkVersion/두 EXE 버전, HANDOFF 검증 기록을 확인하고 그 소스를 기준으로 시작한다. 기존 작업 트리·PR·태그를 변경하거나 main에 자동 병합하지 않는다. v0.1.58은 진단 설명만 정리한 버전이며 수집 경로는 현장 확인된 v0.1.57과 같다.

## 목적과 확인된 범위

퇴근 후 휴대폰의 **사내 KI-Messenger 텍스트**로 업무 워크스테이션의 실행 상태와 시뮬레이션 로그를 확인하는 것이 우선 목적이다. 임의 원격 조작이나 클라우드 화면 분석이 아니다.

| 구성 | 현재 구현/확인 |
|---|---|
| Win7 Master | 개별 자기 대화창만 인식. 새 동일 명령/앞뒤 공백 처리. 텍스트 송수신, 필요시 Send 물리 클릭. 지속 마우스 오버 불필요 |
| 사내 LAN | Win7 Master ↔ Win11 Slave의 인증된 정형 PC 상태 왕복 현장 확인. help/total status/pwrsi 명령 |
| Win11 Slave | 세션의 모든 PowerSI PID를 개별 식별·전경 전환·전체 복사/화면 판독. 결과/취소/실패/진단 ZIP을 PID별 보존 |
| LM Studio | Slave와 동일 PC의 loopback만. RTX A6000, 모델 교체 가능. 최근 경량 Qwen3-VL-8B-Instruct Q4_K_M 사용, thinking OFF 요청/사용자 설정. 실제 서버 적용값은 UNKNOWN 유지 |
| 최신 현장 | v0.1.57에서 두 PowerSI 약17.6초. 내용 있는 창95,672자/1,637줄 자동 복사 + 최신18줄 OCR 문자·숫자 일치. 빈 창은 입력/OCR 생략. 두 창 전환 정상 사용자 확인 |

이전 성공을 다시 시험하지 않는다. 최근 OCR18줄은 **하단 최대256원본 픽셀**에 대한 결과이며 전체1,637줄 OCR이 아니다. 전체 원문은 Ctrl+A/C로 얻는다. PowerSI는 일반적으로 진행률을 명확히 제공하지 않으므로 숫자로 진행률을 추측하지 않는다. HFSS에 어떤 정보가 있는지는 사용자 설명 후 결정한다.

## 기존 수집 경로와 재사용 지점

1. `SlaveForm.ReadOutputBuffer`가 세션별 목록을 한 번 만들고 PID/시작 시각/세션을 고정한다.
2. `PowerSiScreenCapture.PrepareAsync`가 정확한 창·Windows 응답·geometry를 확인한다. 전경 요청1회 → 최대750ms 응답 검사 → 최대1000ms 실제 전경 도달 확인. 대상 창을 최대화/복원/resize하지 않는다.
3. 대상 준비 성공 후에만 자기 Slave 창을 잠시 최소화한다. 표준 텍스트 직독이 불가능한 경우, 로컬 모델이 Output 영역을 찾고 `OutputPaneImage`가 픽셀 본문을 검증한다.
4. `OutputAutoCopy`가 출처·본문·가림·응답·입력 상태를 다시 검사하고 선택 해제 클릭 → 깨끗한 캡처 → Ctrl+A/C → 선택 해제를 수행한다. 새로운 clipboard sequence/소유자를 확인한다.
5. 같은 깨끗한 캡처에서 하단 OCR을 수행한다. 전체 복사 원문은 모델에 주지 않는다. 숫자·문자·순서 그대로 전사하며 요약/추측은 금지한다.
6. 결과는 PID별 원문/이미지/전사본으로 남긴다. 한 PID 실패나 OCR 실패가 다른 PID 또는 이미 얻은 원문을 지우지 않는다. 한 번의 사용자 요청 ZIP에 모든 결과를 담는다.

수정 시 실제 호출 경로부터 추적하고 기존 helper를 재사용한다. 아직 요구가 없는 범용 앱 플러그인 구조나 좌표 상수를 미리 만들지 않는다. HFSS가 표준 텍스트를 노출하는지, 화면 판독/복사가 필요한지는 실제 자료로 판단한다. PowerSI의 클릭 허용을 HFSS의 모든 상태/컨트롤 조작 허용으로 확대하지 않는다.

## 절대 유지할 조건과 미구현

- PowerSI 최대화·복원·크기/위치 변경 금지. 사용자가 창을 준비한다. 정확한 대상의 전경 전환만 허용된다.
- 내부 pending 구간에서 클릭이 crash 위험일 수 있다. 현재 WM_NULL/IsHungAppWindow는 Windows 응답 없음만 감지한다. 내부 위험 구간 완전 판별, 모든 상태에서 클릭 안전 보증은 **미구현**이다. 일부러 위험 구간을 재현하지 않는다.
- 화면/원문은 동일 Slave의 loopback LM Studio 외 서버에 보내지 않는다. 외부 모델 fallback 금지. 화면/모델 텍스트를 실행 명령으로 취급하지 않는다.
- 원문·스크린샷·진단 ZIP·pair 파일·설정·인증 정보는 Git에 넣지 않는다. 공개 저장소 허용은 회사 데이터 공개 허용이 아니다.
- 현재 Slave-only. Master/mobile 재시험은 관련 회귀가 생길 때만. Output 발췌문의 모바일 전달/복수 PID wire 계약은 **아직 미구현**이며 로컬 성공과 혼동하지 않는다.
- 원격 실행·중단, HFSS 수집, 장기 무인 복구는 미구현. 장시간 대기 시험은 사용자가 퇴근 후 설정할 수 있을 때 후순위다.
- .NET Framework4.8/C#7.3/WinForms, Win7 Master/Win11 Slave 유지. 두 EXE가 Link 소스를 각각 포함하므로 공통 변경은 양쪽 빌드 대상이다.

## 새 task에서 적용할 스킬과 협업 방식

스킬의 설치/런타임 상태가 task 간 자동 복제되는 것은 아니다. **아래 스킬을 새 세션에서 찾아 원문 SKILL.md를 읽고 적용한다.** 사용할 수 없으면 미설치 사실과 대체 방법을 짧게 밝힌다. 다른 서비스에서도 아래 원칙은 그대로 사용한다.

| 이번 인계 시 사용한 스킬 | 새 task 적용 |
|---|---|
| `ponytail:ponytail` — full | 구현/수정 시 지속. 기존 코드→표준/플랫폼 기능→최소 변경. 불필요한 구조·의존성 추가 금지. 입력/데이터 보호 검사를 줄이지 않음 |
| `engineering:debug` | 현장 실패는 증거/실제 흐름부터 원인 분리. 필요한 수정과 재현 검사, 배포까지 완료 |
| `engineering:documentation` | HANDOFF/시험 안내를 구현·검증 사실에 맞춰 갱신. 확인/미확인을 구분 |
| `engineering:deploy-checklist` | 해당 범위의 Windows build/self-test/CI/게시 패키지 점검. 필요 없는 승인/장기 대기 절차는 추가하지 않음 |
| `codex-usage-guard:usage-aware-workflow` | 긴 작업 전/전환 시 사용량 확인. 사용자는 이미 필요한 작업을 계속 진행하도록 승인했다. 도구 advisory를 새로운 강제 중단/재승인으로 바꾸지 않음 |

초기에 요청됐던 `integrated-agent-workflow:integrated-agent-flow`, `visualize:visualize`, `i-have-adhd:i-have-adhd`도 프로젝트 선호의 이력이다. 이번 마무리에서 실제 실행한 스킬과 구분한다. 향후 해당 작업에 필요하고 설치돼 있으면 사용하되, 없는 스킬을 상속/사용했다고 주장하지 않는다. 짧은 한국어 설명, 최소 사용자 조작, 필요한 경우만 시각 자료라는 의도는 유지한다.

사용자는 사람의 시간을 특히 중시한다. 통합 가능한 시험은 한 번에, 완료된 기능을 확인하기 위한 동일 반복은 하지 않는다. 필요한 수정을 매번 "진행할까요"로 끊지 말고 검증·다음 시험판까지 이어간다. 단, 새 권한 경계와 사용자가 보류한 HFSS 설명은 존중한다. 독립 병렬 작업이 실제로 도움이 될 때만 subagent를 사용하고 결과를 검토한다.

## 검증과 전달

Windows에서 `scripts/build-package.ps1`로 양쪽 Release/실제 EXE self-test를 확인한다. 대상이 Slave-only인 경우 해당 옵션을 쓸 수 있다. 현장과 다른 자체 테스트의 PASS를 현장 성공으로 쓰지 않고 SKIP를 공개한다.

사용자용 패키지는 기존 GitHub CI `workflow_dispatch`의 새 `release_tag`로 prerelease 게시한다. 정확한 commit/tag/EXE 버전, 공개 다운로드, 해시/ZIP 내용을 확인한다. Slave-only ZIP 직접 링크와 SHA256을 전달한다. 세부 절차와 최종 검증은 HANDOFF §9–10을 따른다. HFSS 상세 설명 전에는 새 현장 시험판이나 기능을 임의 생성하지 않는다.
