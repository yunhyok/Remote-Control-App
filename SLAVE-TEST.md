# Remote Monitor Slave v0.1.51 — 입력 전 확인과 자동 복사 1회

**v0.1.50 현장 시험은 건너뜁니다.** 이번에는 수동 복사로 위치를 한 번 학습하고, 선택 강조를 한 번 해제한 뒤 자동 복사 한 번을 확인합니다. 모델 비교는 같은 OCR 이미지가 있을 때 선택 사항입니다. Master·메신저·모바일, 창 크기 반복 변경, 장시간 대기, SELF-TEST 실행은 필요 없습니다.

v0.1.51은 **클릭하기 전에 새 화면의 Output 본문을 확인**합니다. 선택 강조 때문에 본문을 확인하지 못하면 클릭·키 입력 없이 중단합니다. 따라서 아래의 강조 해제가 필요합니다. **자동 복사가 성공해도 Ctrl+A 선택 강조가 다시 남으므로, 연속 무인 복사가 완성됐다는 뜻은 아닙니다.** 선택된 상태에서의 재확인은 후속 과제입니다.

## 한 번의 시험 세션

1. 기존 Slave만 종료하고 새 ZIP을 새 폴더에 풉니다. EXE와 config를 함께 두고 실행해 제목 표시줄의 **v0.1.51**을 확인합니다. PowerSI 창 하나와 Output을 보이게 두고 시뮬레이션은 그대로 둡니다. Slave의 **LM Studio 설정**에서 로드된 로컬 이미지 모델과 **학습한 Output 위치로 자동 복사** 켜짐을 확인합니다. Qwen thinking 모델은 LM Studio에서 thinking을 끕니다. 앱의 요청 필드를 서버가 존중하는지는 아직 미확인입니다.
2. **두 방식 비교**를 한 번 누릅니다. 수동 복사 안내가 나오면 30초 안에 **Output 본문 클릭 → Ctrl+A → Ctrl+C**를 한 번 합니다. 완료 후 메인 창의 **자동 복사 위치: 학습됨**을 확인합니다. 학습되지 않았다면 반복 복사하지 말고 결과 코드와 진단 묶음을 전달합니다.
3. 학습됐다면 **PowerSI Output 본문을 한 번 클릭해 선택 강조를 해제**합니다. 그 뒤 **두 방식 비교**를 한 번 더 누르고 마우스·키보드를 약10초 동안 건드리지 않습니다. Slave가 잠시 최소화되고 앱은 본문 재확인 후 클릭·Ctrl+A·Ctrl+C를 한 번 시도합니다. `AUTO_COPY_READ / AUTO_CLIPBOARD`면 이번 자동 수집 성공입니다. 실패해도 수동 복사를 다시 요구하지 않고 실패 코드와 확보한 화면을 남깁니다. 이전 원문은 **“이번 자동 복사 실패 — 이전 수집본 참고용”**으로 보존하며 새 수집 성공이 아닙니다. **세 번째 자동 복사는 이번 시험에 추가하지 않습니다.**
4. 결과가 끝나면 **결과 비교 보기**에서 원문, **문자 전사 입력 — 하단 확대**, 전사본을 봅니다. 자동 복사본의 글자·줄 수가 첫 수동 복사본과 비슷한 규모인지, 숫자·단위·최신 줄이 맞는지 확인합니다. 원문 대조의 “누락”은 대응 원문 행을 찾지 못한 **전사 행**이며, 전체 로그의 누락률이나 OCR 정확도가 아닙니다.
5. **선택 사항:** OCR 입력이 만들어졌고 다른 모델과 비교하고 싶다면 비교 창만 닫습니다. LM Studio에서 모델을 교체하고 Slave 설정에서 현재 모델과 단계별 제한(큰 모델은 최대90초)을 저장한 뒤 **같은 화면 재판독**을 한 번 누릅니다. 같은 OCR 이미지에 전사만 다시 요청합니다. 전체 비교·수동 복사·위치 학습을 반복하지 않습니다. OCR 입력이 없다면 이 단계도 생략합니다.
6. 비교 창의 **진단 묶음 저장…**으로 ZIP 하나를 저장하고 **Slave 로그 하나와 ZIP 하나**를 전달합니다. 진단 묶음에는 화면과 Output 원문이 그대로 들어 있으므로 Git이나 외부 공유 채널에 올리지 않습니다. 자동 복사 실패 프레임을 확보했다면 ZIP 안 `auto-copy-frame.png`로 확인할 수 있습니다.

자동 복사 worker 제한은8초이며 Stop·시간 초과 시 입력 worker에 취소를 알리고 **최대1초 정리 시간**을 준 뒤 응답 없는 worker만 종료합니다. 화면 판독은 별도 작업이라 계속될 수 있습니다. 각 모델 호출15~90초, LLM 전체100초, 전체 비교105초입니다. **105초를 넘겨도 진행 중이면 Stop 후 로그를 전달**하세요. 완료 뒤 대기 상태에서 Stop을 누르면 이력이 지워지므로 **진단 묶음을 먼저 저장**합니다. 자동 재시도는 없습니다.

## 결과 코드

| 결과 | 뜻 |
|---|---|
| `AUTO_COPY_READ / AUTO_CLIPBOARD` | 입력 전 본문을 확인하고 클릭·Ctrl+A·Ctrl+C로 새 텍스트를 수집. 전체 선택 여부와 내용은 별도로 확인 |
| `AUTO_COPY_BODY_UNCONFIRMED`, `AUTO_COPY_BODY_MOVED` | **클릭 전** 본문이 불명확하거나 학습 때와 달라짐. 선택 강조도 원인일 수 있음. 클릭·키 없이 중단 |
| `AUTO_COPY_OCCLUDED` | 클릭 지점이 다른 창에 가림. `OCCLUDER\|이름`으로 구분하며 `SELF`는 Slave 자신 |
| `AUTO_COPY_WINDOW_CHANGED` | 대상 인스턴스·창 크기·좌표·표시 상태 변경을 감지해 중단 |
| `AUTO_COPY_INPUT_BUSY`, `AUTO_COPY_CURSOR_*`, `AUTO_COPY_FOREGROUND_*` | 입력 간섭·커서·전경 변화를 감지해 중단 |
| `AUTO_COPY_NO_CLIPBOARD`, `AUTO_COPY_CLIPBOARD_FOREIGN` | 자동 Ctrl+C 직전 기준 이후의 새 클립보드가 없거나 소유자가 다름. 과거 복사본은 성공 처리하지 않음 |
| `AUTO_COPY_TIMEOUT`, `AUTO_COPY_CANCELLED` | 제한 시간/취소. 자동 복사를 반복하지 말고 로그 확인 |
| `USER_COPY_READ / USER_CLIPBOARD` | 동일 PowerSI에서 새 수동 복사본을 수신 |
| `AUTO_COPY_… / PREVIOUS_CAPTURE` | 이번 자동 복사는 실패했으며 이전 원문을 참고용으로 유지. 표시된 원래 수집UTC 확인 |
| `OUTPUT_ANCHOR_LEARNED` / `OUTPUT_ANCHOR_UNCONFIRMED` | 수동 복사 위치가 같은 실행의 화면에서 확인됐는지 여부 |
| `BUFFER_READ / NATIVE_WM_GETTEXT` | 표준 텍스트 객체에서 직접 수집 |
| `BUFFER_OUTPUT_NOT_IDENTIFIED`, `BUFFER_…` | 표준 객체 직독 실패/제한. 첫 실행의 수동 복사 학습 경로와 구분 |
| `OUTPUT_READ` | 모델 텍스트 반환. 정확도나 시뮬레이션 완료 보장은 아님 |
| `LOCATE_OUTPUT_…`, `OUTPUT_REGION_UNCONFIRMED` | 위치/본문 경계 미확정. 원래 제안 상자로 OCR을 강행하지 않음 |
| `READ_CROP_…`, `…_INCOMPLETE_REASONING` | 전사 실패 또는 사고 과정에 토큰을 쓰고 답이 비었음. 확보한 OCR 이미지로 재판독 가능 |
| `TRANSCRIPT_COMPARE` | `T1\|줄수\|일치\|정규화\|근사\|누락\|빈줄\|순서`. 동일 원문 행을 중복 사용하지 않는 부분 대조 |
| `DIAG_BUNDLE_SAVED` / `DIAG_BUNDLE_FAILED` | 사용자가 요청한 진단 묶음 저장 결과 |

원문 수집과 캡처 시각은 다를 수 있습니다. 반면 **고정된 OCR 입력 이미지와 그 전사본의 차이**는 실행 중 로그 갱신으로 설명할 수 없습니다. PowerSI가 버린 과거 로그는 복원하지 못하며 진행률은 추측하지 않습니다. 자동 복사 설정을 끄면 Slave는 입력을 만들지 않고 수동 복사 안내만 사용합니다. 학습 위치는 메모리에만 남습니다.

## 전달할 짧은 설명

- 두 번째 비교의 **최근 자동 복사 코드**와, 성공했다면 전체 텍스트의 글자·줄 수가 첫 복사본과 비슷한지.
- OCR 입력에서 실제로 틀린 숫자·단위·빠진 최신 줄이 있는지.
- 클릭·복사 또는 Slave 창 최소화·복원이 작업에 영향을 주었는지.
