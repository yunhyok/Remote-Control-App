# Remote Monitor Slave v0.1.48 — 같은 OCR 입력으로 E4B / 31B 비교

E4B로 **두 방식 비교 한 번**, 31B Q4_K_M으로 **같은 화면 재판독 한 번**만 합니다. 이번 재판독은 기존 OCR 입력을 그대로 재사용해 **문자 전사만** 실행합니다. Master·메신저·모바일, 창 크기 반복 변경, 재복사, SELF-TEST 실행은 필요 없습니다.

## 실행

1. 기존 Slave만 종료하고 ZIP을 새 폴더에 풉니다. EXE와 config를 함께 두고 실행해 **v0.1.48**을 확인합니다. PowerSI 창 하나와 Output을 보이게 두며 시뮬레이션은 멈추지 않습니다.
2. LM Studio에 E4B를 로드하고 Slave의 **LM Studio 설정**에서 현재 모델을 선택·저장합니다. **두 방식 비교**를 한 번 누릅니다. Slave 수신/IP/연결파일이나 별도의 **PowerSI 확인**은 필요 없습니다.
3. **수동 복사 안내가 나타날 때만** 30초 안에 PowerSI **Output 본문 클릭 → Ctrl+A → Ctrl+C** 합니다. 자동으로 텍스트가 수집되면 아무 조작도 하지 않습니다.
4. 완료 후 **결과 비교 보기**를 엽니다. 오른쪽 기본 이미지인 **Output 전체 본문 — 제목줄 제외**가 본문 아래 끝까지 포함하고 다른 패널을 제외하는지 봅니다. 이미지 선택을 **문자 전사 입력 — 하단 확대**로 바꿔 전사본의 숫자·단위·철자·줄 순서를 바로 위 이미지와 대조합니다. LLM 제안 영역과 PowerSI 전체 원본도 선택할 수 있습니다.
5. **비교 창만 닫고** LM Studio에서 기존 **31B Q4_K_M**으로 교체합니다. Slave의 **LM Studio 설정**에서 현재 모델을 선택하고 **단계별 제한 (초)을 90**으로 저장합니다. 대기 중 **Stop**은 이력을 비우므로 누르지 않습니다. 두 모델 동시 로드, 모델 다운로드, GPU·시뮬레이션 설정 변경은 필요 없습니다.
6. **같은 화면 재판독**을 한 번 누릅니다. 기존 OCR PNG를 현재 31B 모델에 보내 전사만 실행합니다. 새 캡처·위치 찾기·크롭·Ctrl+A/C는 하지 않습니다. 완료 후 비교 창 위쪽 **실행 선택**에서 E4B와 31B 결과를 전환해 실제 입력과 숫자를 대조합니다.

이전 결과에 OCR 이미지가 아직 없으면 위치 찾기부터 실행됩니다. 이번에는 4번에서 **문자 전사 입력**을 확인한 결과를 재사용합니다. 추가 모델 비교를 원할 때만 Qwen을 같은 입력으로 재판독할 수 있으며, 지금은 31B Q4를 먼저 확인합니다. Q8 반복 시험은 필요 없습니다.

본문 크롭은 제목줄을 제외한 원본 픽셀입니다. 실제 OCR 입력은 본문 아래쪽 최대256픽셀 행을2배 픽셀 복제한 이미지이며, 본문 폭이2048픽셀을 넘으면1배입니다. 확대 이미지에 없는 글자는 복원하지 않습니다. LLM은 최근 최대12줄/2000자를 그대로 전사하도록 요청받지만 숫자 정확도와 지시 준수는 아직 미검증입니다. 진행률·완료 여부도 판정하지 않습니다.

왼쪽에는 전체 원문, 본창에는 끝부분4000자 미리보기를 표시합니다. 원문 수집과 캡처 시각은 다를 수 있으므로 **실제 OCR 입력과 전사본을 먼저 대조**하세요. 같은 이미지의 불일치는 시뮬레이션 갱신으로 설명되지 않습니다. 재판독 이력은 최근 최대8개를 Slave 메모리에 유지하고, **Stop**은 재판독을 취소하면서 이전 완료 이력을 보존합니다.

## 결과 구분

| 결과 | 뜻 |
|---|---|
| `BUFFER_READ / NATIVE_WM_GETTEXT` | 표준 텍스트 객체에서 자동 수집 |
| `USER_COPY_READ / USER_CLIPBOARD` | 동일 PowerSI의 새 복사본 수신. Output 전체 선택 여부는 대조 필요 |
| `BUFFER_OUTPUT_NOT_IDENTIFIED`, `BUFFER_…` | 텍스트 수집 실패/제한. 수동 복사 안내가 있으면 같은 실행에서 원문 확보 가능 |
| `OUTPUT_READ` | 모델이 텍스트를 반환함. 정확도나 시뮬레이션 완료 보장은 아님 |
| `LOCATE_OUTPUT_…` | LLM 위치 지정 실패. 원본 확인 가능, OCR 미실행 |
| `CROP_OUTPUT_REGION_BOUNDARY_UNCONFIRMED` | 본문 경계 후보를 하나로 확정하지 못함. 제안 상자로 대신 OCR하지 않음 |
| `READ_CROP_…` | 문자 전사 단계 실패. 생성된 본문/입력 이미지는 비교 화면에서 확인 가능 |
| `…INCOMPLETE_LENGTH` | 해당 실행의 응답이 길이 제한 종료를 명시함. 일반 미완료·시간 초과와 구분 |
| `OUTPUT_UNAVAILABLE`, `VISION_…`, `SC_…` | 판독·대상·세션 등의 확인 불가/오류. 텍스트 수집 결과와 별개 |

각 호출 제한은15~90초(기본60초), LLM 전체100초, 실행 상한105초는 유지됩니다. **31B 재판독은90초로 설정**하며 기존 위치 찾기 약44~50초를 생략하므로 OCR 한 번에 설정된 시간을 사용할 수 있습니다. **105초가 지나도 진행 중이면 Stop 후 로그를 첨부**하세요. 자동 재시도는 없으며 반복 시도나 장시간 대기는 필요 없습니다.

## 전달할 결과

**Slave 로그 하나**와 **① E4B가 만든 본문 경계가 맞는지 ② 동일 OCR 입력에서 31B가 숫자를 그대로 읽었는지 ③ 요약·의역이 있었는지**를 알려주세요. 원문이나 설계 화면은 외부에 첨부할 필요 없습니다. 앱을 종료하지 않고 로그를 첨부할 수 있습니다.

`VISION_RUN`의 **`mode=OCR_ONLY crop_reused=1`**은 기존 OCR PNG를 그대로 사용했다는 뜻이며 `ocr_sample` SHA256이 이전 결과와 같습니다. `mode=LOCATE_OCR crop_reused=0`은 위치 찾기부터 실행한 경우입니다. 모델 ID·이름·키·양자화, 전체 PNG의 `sample`, 제안/본문 좌표, OCR 크기와 소요 시간도 남습니다. 없는 모델·표본·좌표 정보는 `UNKNOWN`입니다. 두 해시는 비교 화면에도 표시하며, 숫자 품질은 같은 `ocr_sample`에서 대조합니다.

최초 `total_ms`에는 캡처 시간이 들어가고 재판독에는 들어가지 않습니다. OCR 입력 재사용은 `locate_ms=0`이며 문자 전사 속도는 `read_ms`로 비교합니다. 모델 목록 조회·HTTP 대기도 포함된 값이며 순수 GPU 속도는 아닙니다. `request_timeout_s=90`으로 저장한 호출 제한도 로그에서 확인할 수 있습니다.

v0.1.47에서 E4B는 두 번 텍스트를 반환했고, 31B Q8/Q4는 OCR 중 전체 100초 제한에 도달했습니다. Qwen은 위치 응답 미완료로 OCR에 진입하지 않았으며 기존 로그만으로 토큰 한도 도달 여부는 알 수 없습니다. **로그에는 문자 정답이 없어 모델 크기별 정확도 우열은 미확인**입니다. 이번에는 같은 입력에 충분한 OCR 시간을 주고 직접 대조합니다. 경계가 불명확하면 원본을 본문으로 대신 표시하지 않습니다.

전체 버퍼·전사본·이미지·실행 이력은 Slave 메모리에서만 표시합니다. 로그에는 비교용 메타데이터만 남고 원문·이미지·프롬프트·토큰은 남기지 않습니다. 전체 버퍼는 LLM·메신저·파일로 보내지 않습니다. 사용자의 Ctrl+C만 일반 Windows 클립보드 내용을 바꾸며 앱은 복사 명령을 생성하지 않습니다. PowerSI가 이미 버린 과거 로그는 복원하지 못합니다.
