# Remote Monitor Slave v0.1.49 — 자동 복사 학습과 원문 대조

**두 방식 비교 두 번**, 필요하면 **같은 화면 재판독 한 번**입니다. 첫 번째 실행의 수동 복사 한 번이 자동 복사 위치를 가르치고, 두 번째 실행에서 앱이 스스로 복사합니다. Master·메신저·모바일, 창 크기 반복 변경, SELF-TEST 실행은 필요 없습니다.

**이번 버전의 자동 복사·원문 대조·진단 묶음은 현장에서 처음 확인하는 기능입니다.** 실패해도 이전과 같은 수동 복사 안내로 돌아가므로 시뮬레이션에는 영향이 없습니다.

## 실행

1. 기존 Slave만 종료하고 ZIP을 새 폴더에 풉니다. EXE와 config를 함께 두고 실행해 제목 표시줄에서 **v0.1.49**를 확인합니다. PowerSI 창 하나와 Output을 보이게 두며 시뮬레이션은 멈추지 않습니다. LM Studio에 **E4B**를 로드하고 Slave의 **LM Studio 설정**에서 현재 모델을 선택·저장합니다. 같은 창의 **학습한 Output 위치로 자동 복사**가 켜져 있는지 확인합니다.
2. **두 방식 비교**를 한 번 누릅니다. 수동 복사 안내가 나오면 30초 안에 **PowerSI Output 본문 안쪽을 클릭 → Ctrl+A → Ctrl+C** 합니다. 이때 클릭한 지점이 자동 복사 위치로 학습됩니다. 자동으로 텍스트가 수집되면 아무 조작도 하지 않습니다. 완료 후 메인 창 아래쪽 **자동 복사 위치**가 **학습됨**인지 봅니다.
3. **두 방식 비교**를 한 번 더 누릅니다. 이번에는 **마우스·키보드를 약10초 동안 건드리지 마세요.** 앱이 창과 본문을 다시 확인한 뒤 한 번 클릭하고 Ctrl+A·Ctrl+C를 보냅니다. 결과에 `AUTO_COPY_READ / AUTO_CLIPBOARD`가 나오면 성공입니다. 다시 `OUTPUT_COPY_WAIT` 수동 복사 안내가 나오면 평소대로 수동 복사하고 **그 사실과 표시된 코드**를 적어 주세요. 커서가 잠시 움직였다가 제자리로 돌아오고 클립보드 내용이 바뀝니다.
4. (OCR 입력이 만들어졌을 때만) **비교 창만 닫고** LM Studio에서 **31B Q4_K_M**으로 교체한 뒤 Slave 설정에서 현재 모델과 **단계별 제한 90초**를 저장하고 **같은 화면 재판독**을 한 번 누릅니다. 새 캡처·위치 찾기·복사는 하지 않습니다. 경계 확정에 실패해 OCR 입력이 없으면 이 단계는 건너뜁니다.
5. **결과 비교 보기**를 엽니다. 오른쪽 위는 이미지와 전사본, 그 아래가 새 **원문 대조**입니다. 일치·정규화 일치·근사·누락 수와 어긋난 줄을 확인합니다. 실행 선택 상자로 모델별 결과를 전환할 수 있습니다.
6. 같은 창 아래의 **진단 묶음 저장…**을 누르고 경고를 확인한 뒤 ZIP을 저장합니다. **Slave 로그 하나와 이 ZIP**을 전달해 주세요. ZIP에는 화면 이미지와 Output 전체 텍스트가 그대로 들어 있으므로 **Git이나 외부 채널에 올리지 말고** 필요한 담당자에게만 직접 전달합니다.

자동 복사를 원하지 않으면 **LM Studio 설정**에서 체크를 끄십시오. 그러면 이전 버전과 똑같이 앱이 입력을 만들지 않습니다. 위치는 메모리에만 저장되어 Slave를 닫거나 PowerSI를 다시 시작하면 사라지고, 다음 수동 복사에서 다시 배웁니다.

본문 크롭은 제목줄을 제외한 원본 픽셀입니다. 실제 OCR 입력은 본문 아래쪽 최대256픽셀 행을2배 픽셀 복제한 이미지이며, 본문 폭이2048픽셀을 넘으면1배입니다. LLM은 최근 최대12줄/2000자를 그대로 전사하도록 요청받지만 숫자 정확도와 지시 준수는 아직 미검증입니다. 진행률·완료 여부는 판정하지 않습니다. 재판독 이력은 최근 최대8개를 Slave 메모리에 유지하고, **Stop**은 재판독을 취소하면서 이전 완료 이력을 보존합니다. 대기 중 Stop은 이력을 비웁니다.

## 결과 구분

| 결과 | 뜻 |
|---|---|
| `BUFFER_READ / NATIVE_WM_GETTEXT` | 표준 텍스트 객체에서 자동 수집 |
| `AUTO_COPY_READ / AUTO_CLIPBOARD` | 학습한 위치를 다시 확인하고 앱이 클릭·Ctrl+A·Ctrl+C를 한 번 실행해 수집. 전체 선택 여부는 원문 대조로 확인 |
| `AUTO_COPY_WINDOW_CHANGED`, `AUTO_COPY_BODY_UNCONFIRMED` | 창/클라이언트 크기/본문 경계가 학습 때와 달라 중단. 입력을 보내지 않음 |
| `AUTO_COPY_INPUT_BUSY`, `AUTO_COPY_OCCLUDED`, `AUTO_COPY_CURSOR_*`, `AUTO_COPY_FOREGROUND_*` | 사용자의 입력·다른 창·전경 변화로 중단. 수동 복사 안내로 복귀 |
| `AUTO_COPY_NO_CLIPBOARD`, `AUTO_COPY_CLIPBOARD_FOREIGN`, `AUTO_COPY_TIMEOUT` | 새 클립보드가 없거나 다른 프로그램 소유. 오래된/다른 복사본은 성공 처리하지 않음 |
| `USER_COPY_READ / USER_CLIPBOARD` | 동일 PowerSI의 새 수동 복사본 수신. 이 실행에서 자동 복사 위치를 학습 |
| `OUTPUT_ANCHOR_LEARNED` / `OUTPUT_ANCHOR_UNCONFIRMED` | 클릭 위치를 같은 실행의 화면에서 확인했는지 여부(`reason=FRAME_MISSING/SIZE_MISMATCH/BODY`) |
| `BUFFER_OUTPUT_NOT_IDENTIFIED`, `BUFFER_…` | 표준 객체 직독 실패/제한. 자동 또는 수동 복사로 원문 확보 가능 |
| `OUTPUT_READ` | 모델이 텍스트를 반환함. 정확도나 시뮬레이션 완료 보장은 아님 |
| `LOCATE_OUTPUT_…` | LLM 위치 지정 실패. 원본 확인 가능, OCR 미실행 |
| `OUTPUT_REGION_UNCONFIRMED` (`CROP_OUTPUT_REGION_BOUNDARY_UNCONFIRMED`) | 본문 경계 후보를 하나로 확정하지 못함. `body=B2\|…` 수치로 원인 구분. 제안 상자로 대신 OCR하지 않음 |
| `READ_CROP_…` | 문자 전사 단계 실패. 생성된 본문/입력 이미지는 비교 화면에서 확인 가능 |
| `TRANSCRIPT_COMPARE` | 원문 대조 결과 `T1\|줄수\|일치\|정규화\|근사\|누락\|빈줄\|순서` |
| `DIAG_BUNDLE_SAVED` / `DIAG_BUNDLE_FAILED` | 진단 묶음 저장 결과. 크기·SHA256만 기록하고 경로는 남기지 않음 |
| `OUTPUT_UNAVAILABLE`, `VISION_…`, `SC_…` | 판독·대상·세션 등의 확인 불가/오류. 텍스트 수집 결과와 별개 |

각 호출 제한은15~90초(기본60초), LLM 전체100초, 실행 상한105초이며 자동 복사 worker는 최대8초입니다. **105초가 지나도 진행 중이면 Stop 후 로그를 첨부**하세요. 자동 재시도는 없습니다.

## 전달할 결과

**Slave 로그 하나**, **진단 묶음 ZIP 하나**와 다음을 알려주세요.

1. 두 번째 **두 방식 비교**에서 `AUTO_COPY_READ`가 나왔는지, 아니면 어떤 `AUTO_COPY_*` 코드로 중단됐는지.
2. 자동 복사본의 글자·줄 수가 첫 번째 수동 복사본과 비슷한지(Output 전체가 선택됐는지).
3. **원문 대조**에서 누락·근사로 표시된 줄이 실제로 틀린 숫자인지, 아니면 화면 갱신 때문인지.
4. 클릭·복사 때 PowerSI 동작에 이상이 없었는지(선택 해제, 포커스 이동 등).

로그에는 비교용 메타데이터만 남고 원문·이미지·프롬프트·토큰은 남지 않습니다. 반대로 진단 묶음 ZIP에는 화면과 원문이 그대로 들어 있으니 전달 범위를 직접 정하세요. PowerSI가 이미 버린 과거 로그는 복원하지 못합니다.
