# Remote Monitor Slave v0.1.53 — Qwen3-VL 8B와 선택 정리 확인

**새 앱 수동 설정 한 번 + 같은 모델의 새 자동 비교 한 번**으로 PowerSI의 남은 확인을 마무리합니다. 자동 복사 시작·종료의 선택 해제와 OCR을 함께 봅니다. Master·메신저·모바일, 창 크기 반복 변경, 장시간 대기, SELF-TEST 실행은 필요 없습니다.

v0.1.52 성공 결과4개는 OCR 입력18줄을 모두 전사했고 숫자·문자는 같으며 들여쓰기 차이6줄만 있었습니다. 사용자가 **모든 실행에서 thinking을 직접 껐다**고 확인했습니다. 이번에는 접두부 없는 좌표를 받도록 고친 **Qwen3-VL 8B를 우선** 재확인합니다. v0.1.53의 새 동작은 현장 미검증입니다.

## 한 번의 짧은 세션

1. 기존 Slave만 종료하고 새 ZIP을 새 폴더에 풉니다. EXE와 config를 함께 두고 실행해 제목 표시줄의 **v0.1.53**을 확인합니다. PowerSI 창 하나와 Output을 보이게 둡니다. LM Studio에서 로컬 Qwen3-VL 8B를 로드하고 thinking OFF를 유지합니다. Slave의 **LM Studio 설정**에 모델과 호출 제한을 저장하고 자동 복사는 켜 둡니다. LM Studio 창이 Output을 가리지 않게 옮깁니다.
2. **새 화면 두 방식 비교**를 누릅니다. 수동 안내가 나오면30초 안에 **Output 본문 클릭 → Ctrl+A → Ctrl+C**를 한 번 합니다. 앱이 위치를 배우며 상태줄에 **자동 복사 위치: 학습됨**을 표시합니다. **복사 후 파란 선택 강조는 그대로 둡니다.** 이번 수동 설정은 복사 전 캡처를 사용하며 강조를 정리하지 않습니다.
3. 비교 창만 닫고 **같은 모델로 새 화면 두 방식 비교를 한 번 더** 누릅니다. 자동 복사 중에는 마우스·키보드를 건드리지 않습니다. 앱이 본문 검증 후 선택을 지우고 깨끗한 프레임을 확보한 뒤 Ctrl+A/C와 선택 정리 클릭을 수행합니다. 완료 후 **자동 복사 성공, 새 원문, 파란 강조가 남지 않음**을 확인합니다. 기본 **문자 전사 실제 입력**에 선택 강조가 없는지, 전사본이 이미지 첫 줄부터 마지막 줄까지 숫자·단위·문구를 빠뜨리지 않았는지 함께 봅니다.
4. **진단 묶음 저장…**으로 ZIP 하나를 저장하고 Slave 로그와 함께 전달합니다. 다른 모델을 추가 비교하고 싶을 때만 비교 창을 닫고 모델/thinking OFF를 설정한 뒤 **같은 OCR 이미지 재판독**을 사용합니다. 모델 교체마다 새 비교·복사를 반복할 필요는 없습니다. 저장 전 대기 중 Stop이나 앱 종료를 하지 마세요.

학습이 안 되거나 자동 복사/영역 확인에 실패하면 반복 조작 대신 진단을 저장합니다. 자동 실패 뒤 앱은 수동 복사를 다시 요구하지 않습니다. 이전 원문이 표시되면 `PREVIOUS_CAPTURE` 참고용이며 이번 수집 성공이 아닙니다.

OCR 입력은 **Output 본문 하단 최대256픽셀**의 확대 이미지입니다. 전체 Output 본문이나 전체 복사 버퍼가 아닙니다. **원문 대응 없음=0**도 이미지의 모든 줄을 읽었다는 뜻은 아니므로 이미지와 전사본을 대조합니다. 거부된 응답은 UNVALIDATED로만 표시하며 OCR 대조/전사 파일에 포함하지 않습니다.

## 선택적 모델 재판독과 시간 제한

**같은 OCR 이미지 재판독**은 저장한 PNG 바이트를 그대로 사용합니다. OCR 입력이 없는 결과에서는 버튼이 **저장 화면 재판독**으로 바뀌며 영역 찾기부터 실행하므로 동일 OCR 입력 비교와 구분합니다. 최근 최대8개 실행이 메모리에 남으므로 오래된 결과가 밀려나거나 Stop/앱 종료로 지워지기 전에 진단을 저장합니다.

앱의 `thinking_requested=off thinking_effective=UNKNOWN`은 OFF 요청과 앱이 확인할 수 없는 서버 적용을 구분합니다. 사용자의 실제 OFF 확인을 부정하는 기록이 아닙니다. `model_id`/양자화·`ocr_sample`·`read_ms`로 같은 입력의 모델 결과를 비교하며, 재판독의 위치 좌표는 이전 실행에서 물려받습니다.

자동 복사는8초, 취소/시간 초과 뒤 입력 worker 정리는 최대1초입니다. 모델 호출은15~90초(기본60), LLM 판독은100초입니다. **최초 설정·재판독은 전체105초, 학습된 자동 비교는 최대125초**이며 앱의 현재 제한 표시를 따릅니다. 표시된 최대 시간을 넘겨도 진행 중이면 Stop 후 로그를 전달합니다. 장시간 대기 시험을 추가하지 않으며 자동 재시도는 없습니다.

## 결과 코드

| 결과 | 뜻 |
|---|---|
| `AUTO_COPY_READ / AUTO_CLIPBOARD` | 새 원문 수집과 보호 검사를 통과한 선택 정리 완료. 실제 전체 선택/선택 해제/OCR 내용은 화면 확인 |
| `AUTO_COPY_BODY_UNCONFIRMED`, `AUTO_COPY_BODY_MOVED` | 본문/학습 경계 확인 실패. 최초 검사 실패면 입력 없이, 선택 해제 후 재검사 실패면 Ctrl+A/C 없이 중단 |
| `AUTO_COPY_OCCLUDED` | 다른 창이 클릭 지점을 가림. `OCCLUDER\|이름`으로 구분하며 `SELF`는 Slave |
| `AUTO_COPY_WINDOW_CHANGED`, `AUTO_COPY_INPUT_BUSY`, `AUTO_COPY_CURSOR_*`, `AUTO_COPY_FOREGROUND_*` | 대상·좌표·전경·사용자 입력 변화로 중단 |
| `AUTO_COPY_NO_CLIPBOARD`, `AUTO_COPY_CLIPBOARD_FOREIGN` | Ctrl+C 직전 기준 이후의 새 클립보드가 없거나 소유자가 다름 |
| `AUTO_COPY_TIMEOUT`, `AUTO_COPY_CANCELLED` | 제한 시간/취소. 자동 복사 반복 대신 진단 저장 |
| `USER_COPY_READ / USER_CLIPBOARD` | 동일 PowerSI의 새 수동 복사본 수신 |
| `AUTO_COPY_… / PREVIOUS_CAPTURE` | 이번 자동 복사 실패, 원래 수집UTC를 가진 이전 원문 참고용 |
| `OUTPUT_ANCHOR_LEARNED` / `OUTPUT_ANCHOR_UNCONFIRMED` | 같은 실행의 프레임에서 수동 복사 위치 확인 여부 |
| `BUFFER_READ / NATIVE_WM_GETTEXT` | 표준 객체에서 직접 수집 |
| `BUFFER_OUTPUT_NOT_IDENTIFIED`, `BUFFER_…` | 직독 실패/제한. 최초 수동 설정 경로와 구분 |
| `OUTPUT_READ` | 모델 텍스트 반환. 정확도나 시뮬레이션 완료 보장은 아님 |
| `LOCATE_OUTPUT_…`, `OUTPUT_REGION_UNCONFIRMED` | 위치/본문 경계 미확정. 원래 제안 상자로 OCR을 강행하지 않음 |
| `READ_CROP_…`, `…_INCOMPLETE_REASONING` | 전사 실패/미완료. 확보한 OCR 이미지가 있으면 재판독 가능 |
| `TRANSCRIPT_COMPARE` | 원문 행 대응 수치. 전체 OCR 누락률이 아님 |
| `VISION_RUN` | 모델·이미지 해시·시간·전사 정책·thinking 요청 메타데이터 |
| `DIAG_BUNDLE_SAVED` / `DIAG_BUNDLE_FAILED` | 사용자가 요청한 진단 묶음 저장 결과 |

고정 OCR 이미지와 그 전사본의 차이는 실행 중 로그 갱신으로 설명할 수 없습니다. PowerSI가 버린 로그는 복원하지 못하며 진행률은 추측하지 않습니다. 진단 ZIP에는 화면과 Output 원문이 들어 있으므로 Git이나 외부 공유 채널에 올리지 않습니다.

## 전달할 짧은 설명

- Qwen3-VL 8B의 자동 비교 성공 여부, 완료 뒤 선택 강조가 남았는지.
- 실제 OCR 입력의 선택 강조·틀린 숫자/단위·빠진 첫/마지막 줄 여부와 판독 속도.
