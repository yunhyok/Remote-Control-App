# Remote Monitor Slave v0.1.52 — 같은 OCR 이미지로 여러 모델 비교

이번에는 **기준 이미지와 원문을 한 번 확보한 뒤, 모델만 바꿔 같은 이미지를 재판독**합니다. v0.1.51에서 자동 복사 한 번은 현장 성공했으므로 별도의 자동 복사 반복 시험을 하지 않습니다. Master·메신저·모바일, 창 크기 반복 변경, 장시간 대기, SELF-TEST 실행은 필요 없습니다.

v0.1.52는 기존 “최신12줄” 제한 대신 **실제 OCR 이미지 안에 보이는 모든 줄**을 그대로 요청하며, 정상 전사본을12줄/2000자로 자르지 않습니다. OCR 이미지는 여전히 **Output 본문 하단 최대256픽셀을 확대한 것**으로, 전체 Output 본문이나 전체 복사 버퍼를 뜻하지 않습니다. 모델의 생략·숫자 오류는 별도로 확인해야 합니다.

## 한 번의 시험 세션

기준 캡처 전에 Output 텍스트의 파란 선택 강조가 남아 있으면 본문을 한 번 클릭해 해제합니다. 이후 모델 재판독에는 PowerSI 조작이 필요 없습니다.

1. 기존 Slave만 종료하고 새 ZIP을 새 폴더에 풉니다. EXE와 config를 함께 두고 실행해 제목 표시줄의 **v0.1.52**를 확인합니다. PowerSI 창 하나와 Output을 보이게 두고 시뮬레이션은 그대로 둡니다. LM Studio에서 첫 로컬 이미지 모델을 로드하고 **thinking을 끕니다.** Slave의 **LM Studio 설정**에서 해당 모델과 호출 제한을 저장합니다. 모델별 속도를 비교하려면 같은 제한(기본60초, 필요 시 최대90초)을 사용합니다.
2. **두 방식 비교**를 한 번 누릅니다. 수동 복사 안내가 나오면 30초 안에 **Output 본문 클릭 → Ctrl+A → Ctrl+C**를 한 번 합니다. 새 앱의 위치 학습도 이때 함께 진행되지만, 이번에는 학습 확인용 두 번째 비교를 추가하지 않습니다.
3. 완료 후 **결과 비교 보기**의 기본 이미지인 **문자 전사 실제 입력 — 하단 최대 256픽셀 / 이미지 안의 모든 줄 판독**와 전사본을 대조합니다. **이미지 첫 줄부터 마지막 줄까지**, 특히 마지막 숫자·단위·완료 문구가 빠지지 않았는지 봅니다. 원문 대조의 **원문 대응 없음**은 대응 원문을 찾지 못한 전사 행 수입니다. 0이어도 이미지 전체를 빠짐없이 읽었다는 뜻은 아닙니다.
4. 비교 창만 닫고 LM Studio에서 다음 모델로 교체한 뒤 **thinking OFF를 다시 확인**합니다. Slave 설정에서 새 모델을 선택·저장하고 **같은 화면 재판독**을 누릅니다. 이후 모델마다 이 단계만 반복합니다. **두 방식 비교·수동 복사·위치 학습은 반복하지 않습니다.** OCR 입력이 생성되지 않은 첫 실행이면 동등한 OCR 비교가 불가능하므로 진단을 저장합니다.
5. 최근 **최대8개 실행(기준 실행 포함)**이 보존됩니다. 원하는 모델 비교가 끝나면 **진단 묶음 저장…**으로 ZIP 하나를 저장하고 Slave 로그와 함께 전달합니다. 8개를 넘길 때는 오래된 결과가 밀려나기 전에 ZIP을 먼저 저장합니다. 완료 후 대기 중 Stop이나 앱 종료 전에 저장하세요. 진단 묶음에는 화면과 Output 원문이 그대로 들어 있으므로 Git이나 외부 공유 채널에 올리지 않습니다.

앱은 위치/OCR 요청 모두 `enable_thinking=false`를 보내지만 서버가 실제로 적용했는지 판정하지 않습니다. 로그의 `thinking_requested=off thinking_effective=UNKNOWN`은 이 차이를 나타냅니다. LM Studio의 모델별 실제 설정은 사용자가 확인합니다. `model_id`/양자화와 `ocr_sample` 해시, `read_ms`를 함께 보면 같은 입력에서 모델별 결과를 구분할 수 있습니다. 재판독의 위치 좌표는 첫 실행에서 물려받으므로 이번 비교는 **문자 판독 비교**이지 모델별 위치 찾기 비교가 아닙니다.

자동 복사 worker 제한은8초이며 Stop·시간 초과 시 입력 worker에 취소를 알리고 **최대1초 정리 시간**을 준 뒤 응답 없는 worker만 종료합니다. 화면 판독은 별도 작업이라 계속될 수 있습니다. 각 모델 호출15~90초, LLM 전체100초, 전체 비교105초입니다. **105초를 넘겨도 진행 중이면 Stop 후 로그를 전달**하세요. 완료 뒤 대기 상태에서 Stop을 누르면 이력이 지워지므로 **진단 묶음을 먼저 저장**합니다. 자동 재시도는 없습니다.

## 결과 코드

| 결과 | 뜻 |
|---|---|
| `AUTO_COPY_READ / AUTO_CLIPBOARD` | 입력 전 본문을 확인하고 클릭·Ctrl+A·Ctrl+C로 새 텍스트를 수집. 전체 선택 여부와 내용은 별도로 확인 |
| `AUTO_COPY_BODY_UNCONFIRMED`, `AUTO_COPY_BODY_MOVED` | **클릭 전** 본문이 불명확하거나 학습 때와 달라짐. 선택 강조도 원인일 수 있음. 클릭·키 없이 중단 |
| `AUTO_COPY_OCCLUDED` | 클릭 지점이 다른 창에 가림. `OCCLUDER\|이름`으로 구분하며 `SELF`는 Slave 자신 |
| `AUTO_COPY_WINDOW_CHANGED` | 대상 인스턴스·창 크기·좌표·표시 상태 변경을 감지해 중단 |
| `AUTO_COPY_INPUT_BUSY`, `AUTO_COPY_CURSOR_*`, `AUTO_COPY_FOREGROUND_*` | 입력 간섭·커서·전경 변화를 감지해 중단 |
| `AUTO_COPY_NO_CLIPBOARD`, `AUTO_COPY_CLIPBOARD_FOREIGN` | 자동 Ctrl+C 직전 기준 이후의 새 클립보드가 없거나 소유자가 다름. 과거 복사본은 성공 처리하지 않음 |
| `AUTO_COPY_TIMEOUT`, `AUTO_COPY_CANCELLED` | 제한 시간/취소. 자동 복사를 반복하지 말고 로그 확인 |
| `USER_COPY_READ / USER_CLIPBOARD` | 동일 PowerSI에서 새 수동 복사본을 수신 |
| `AUTO_COPY_… / PREVIOUS_CAPTURE` | 이번 자동 복사는 실패했으며 이전 원문을 참고용으로 유지. 표시된 원래 수집UTC 확인 |
| `OUTPUT_ANCHOR_LEARNED` / `OUTPUT_ANCHOR_UNCONFIRMED` | 수동 복사 위치가 같은 실행의 화면에서 확인됐는지 여부 |
| `BUFFER_READ / NATIVE_WM_GETTEXT` | 표준 텍스트 객체에서 직접 수집 |
| `BUFFER_OUTPUT_NOT_IDENTIFIED`, `BUFFER_…` | 표준 객체 직독 실패/제한. 첫 실행의 수동 복사 학습 경로와 구분 |
| `OUTPUT_READ` | 모델 텍스트 반환. 정확도나 시뮬레이션 완료 보장은 아님 |
| `LOCATE_OUTPUT_…`, `OUTPUT_REGION_UNCONFIRMED` | 위치/본문 경계 미확정. 원래 제안 상자로 OCR을 강행하지 않음 |
| `READ_CROP_…`, `…_INCOMPLETE_REASONING` | 전사 실패 또는 사고 과정에 토큰을 쓰고 답이 비었음. 확보한 OCR 이미지로 재판독 가능 |
| `TRANSCRIPT_COMPARE` | `T1\|줄수\|일치\|정규화\|근사\|원문 대응 없음\|빈줄\|순서`. 형식은 기존과 동일하며 전체 OCR 누락률은 아님 |
| `VISION_RUN` | `transcript_policy=ALL_VISIBLE_V1`, `thinking_requested=off`, `thinking_effective=UNKNOWN`, `ocr_max_tokens=4096`과 모델/해시/시간 메타데이터 |
| `DIAG_BUNDLE_SAVED` / `DIAG_BUNDLE_FAILED` | 사용자가 요청한 진단 묶음 저장 결과 |

원문 수집과 캡처 시각은 다를 수 있습니다. 반면 **고정된 OCR 입력 이미지와 그 전사본의 차이**는 실행 중 로그 갱신으로 설명할 수 없습니다. PowerSI가 버린 과거 로그는 복원하지 못하며 진행률은 추측하지 않습니다. 자동 복사 설정을 끄면 Slave는 입력을 만들지 않고 수동 복사 안내만 사용합니다. 학습 위치는 메모리에만 남습니다. 선택 강조 상태의 재확인·연속 무인 복사는 미완료이며 이번 모델 비교에서는 시험하지 않습니다.

## 전달할 짧은 설명

- 모델별로 thinking을 껐는지와 결과에 틀린 숫자·단위·빠진 첫/마지막 줄이 있었는지.
- 응답 속도와 전체 줄 판독이 가장 나았던 모델. 모델 ID·양자화·시간은 진단 묶음으로 함께 비교합니다.
